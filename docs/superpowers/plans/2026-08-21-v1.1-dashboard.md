# v1.1 投资仪表盘 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 空会话欢迎区换成顾问仪表盘（当前组合总览、今日简报、待关注、三条场景入口）；点评仅点击后用 LLM 生成一句中文。

**Architecture:** Python `dashboard.py` 汇总组合/指数/新闻/预警；`GET /dashboard/summary` 一次返回。`POST /portfolio/active` 持久化当前组合。`POST /dashboard/comment` 单独打 LLM，不写会话。渲染层用 `DashboardWelcome` 替换 `ChatView` 的 hero 与欢迎快捷语句。

**Tech Stack:** Electron IPC、React + TypeScript、`python/api.py`、`PortfolioManager`、`NotifiedNewsStore`、`AlertHistoryStore`、`LLMFactory`。

**Spec:** `docs/superpowers/specs/2026-08-21-v1.1-dashboard-design.md`

## Global Constraints

- 仪表盘嵌在 `#/chat` 空会话，不新开 `/dashboard`。
- 四块全做；原欢迎语和默认快捷语句去掉。
- 点评仅点击「生成点评」才请求；不按日缓存；不写入 session。
- 无持仓：总览引导添加；简报仍放大盘；新闻持仓 → 关注板块 → 全市场。
- 总览可切换组合并 `POST /portfolio/active`；默认 `active_portfolio_id`。
- 大盘固定沪深300（`000300.SH`）。
- 不按 `experience_level` 换入口；不做真实日历（仅文案占位）。
- 面向用户文案用简体中文。
- 不生成 Mock；不编译项目；不写自动化测试；实现后由用户按 spec §9 手工验收。
- 未经用户明确要求不要 git commit。

---

## File Map

| 文件 | 职责 |
|------|------|
| `python/fin-agent/fin_agent/portfolio.py` | `_batch_prices` 带上 `pre_close` |
| `python/fin-agent/fin_agent/dashboard.py` | **新建** summary + comment |
| `python/api.py` | `GET /dashboard/summary`、`POST /dashboard/comment`、`POST /portfolio/active` |
| `src/main/index.ts` | IPC |
| `src/preload/index.ts` / `src/renderer/src/env.d.ts` | 类型与 `window.api` |
| `src/renderer/src/utils/chatPrefill.ts` | `buildWatchTodayPrefill` |
| `src/renderer/src/components/StockSearchModal.tsx` | 可选 `onPick` |
| `src/renderer/src/components/DashboardWelcome.tsx` | **新建** 四块 UI |
| `src/renderer/src/components/ChatView.tsx` | 空会话渲染仪表盘；欢迎快捷语句为空 |
| `src/renderer/src/index.css` | 仪表盘布局 |

---

### Task 1: 后端 summary + 切换组合

**Files:**
- Modify: `python/fin-agent/fin_agent/portfolio.py`（`_batch_prices`）
- Create: `python/fin-agent/fin_agent/dashboard.py`
- Modify: `python/api.py`

**Interfaces:**
- Consumes: `PortfolioManager.get_portfolio_status` / `list_portfolios` / `set_active_portfolio`；`NotifiedNewsStore.list_news`；`query_news_live`；`AlertHistoryStore.list_items`；`UserProfileManager.get_profile`；`get_provider().get_realtime_price`
- Produces:
  - `build_dashboard_summary(portfolio_id=None) -> dict`（`ok: True` 及 spec §6.1 字段）
  - `POST /portfolio/active`
  - `GET /dashboard/summary`

- [ ] **Step 1: 扩展 `_batch_prices`，带上昨收**

把 `python/fin-agent/fin_agent/portfolio.py` 的 `fetch()` 返回值改为同时包含 `pre_close`（列缺失或空则 `0.0`）：

```python
            df = get_provider().get_realtime_price(ts_codes)
            out = {}
            for _, row in df.iterrows():
                pre = row["pre_close"] if "pre_close" in row.index else 0
                out[row["ts_code"]] = {
                    "price": float(row["price"] or 0),
                    "pre_close": float(pre or 0),
                    "name": row.get("name"),
                }
            return out
```

`get_portfolio_status` 仍只用 `price`/`name`，行为不变。

- [ ] **Step 2: 新建 `python/fin-agent/fin_agent/dashboard.py`**

全文如下（不要拆到别的模块；不要 Mock）：

```python
from __future__ import annotations

from fin_agent.alert_history import AlertHistoryStore
from fin_agent.news_store import NotifiedNewsStore
from fin_agent.portfolio import PortfolioManager
from fin_agent.user_profile import UserProfileManager

HS300 = "000300.SH"
HS300_NAME = "沪深300"


def _six_digit(ts_code: str) -> str:
    base = (ts_code or "").split(".", 1)[0].strip()
    return base if base.isdigit() and len(base) == 6 else ""


def _news_text(item: dict) -> str:
    return " ".join(
        [
            str(item.get("title") or ""),
            str(item.get("summary") or ""),
        ]
    )


def _match_holdings(text: str, holdings: list[dict]) -> bool:
    hay = (text or "").lower()
    if not hay:
        return False
    for h in holdings:
        ts_code = str(h.get("ts_code") or "").strip()
        name = str(h.get("name") or "").strip()
        code6 = _six_digit(ts_code)
        if name and name.lower() in hay:
            return True
        if code6 and code6.lower() in hay:
            return True
        if ts_code and ts_code.lower() in hay:
            return True
    return False


def _match_sectors(text: str, sectors: list) -> bool:
    hay = (text or "").lower()
    for s in sectors or []:
        name = str(s or "").strip()
        if name and name.lower() in hay:
            return True
    return False


def _map_local_news(item: dict, match: str) -> dict:
    return {
        "id": item.get("id") or "",
        "title": item.get("title") or "",
        "source": item.get("source") or "",
        "url": item.get("url") or "",
        "match": match,
    }


def _pick_news(holdings: list[dict], sectors: list) -> tuple[str, list]:
    store = NotifiedNewsStore()
    local = store.list_news(offset=0, limit=80).get("items") or []
    if holdings:
        hit = [it for it in local if _match_holdings(_news_text(it), holdings)]
        if hit:
            return "holding", [_map_local_news(it, "holding") for it in hit[:3]]
    if sectors:
        hit = [it for it in local if _match_sectors(_news_text(it), sectors)]
        if hit:
            return "sector", [_map_local_news(it, "sector") for it in hit[:3]]
    if local:
        return "market", [_map_local_news(it, "market") for it in local[:3]]
    try:
        from fin_agent.news_query import query_news_live

        live = query_news_live(hours=24, limit=3)
        items = live.get("items") or []
        out = []
        for it in items[:3]:
            out.append(
                {
                    "id": "",
                    "title": it.get("title") or "",
                    "source": it.get("source") or "",
                    "url": it.get("url") or "",
                    "match": "market",
                }
            )
        return "market", out
    except Exception:
        return "market", []


def _index_snapshot() -> dict | None:
    try:
        from fin_agent.datasources import get_provider

        df = get_provider().get_realtime_price([HS300])
        if df is None or len(df) == 0:
            return None
        row = df.iloc[0]
        price = float(row.get("price") or 0)
        pct = row["pct_chg"] if "pct_chg" in row.index else None
        if pct is None:
            pre = float(row["pre_close"] or 0) if "pre_close" in row.index else 0
            pct = ((price - pre) / pre * 100) if pre else 0.0
        return {
            "ts_code": HS300,
            "name": str(row.get("name") or HS300_NAME),
            "price": round(price, 2),
            "change_pct": round(float(pct or 0), 2),
        }
    except Exception:
        return None


def _today_pnl(positions: list[dict], price_map: dict) -> tuple[float | None, float | None]:
    pnl = 0.0
    used_mv = 0.0
    any_row = False
    for pos in positions:
        ts_code = pos.get("ts_code")
        amount = float(pos.get("amount") or 0)
        quote = price_map.get(ts_code) or {}
        price = float(quote.get("price") or 0)
        pre = float(quote.get("pre_close") or 0)
        if amount <= 0 or price <= 0 or pre <= 0:
            continue
        any_row = True
        pnl += (price - pre) * amount
        used_mv += pre * amount
    if not any_row:
        return None, None
    pct = round(pnl / used_mv * 100, 2) if used_mv else None
    return round(pnl, 2), pct


def build_dashboard_summary(portfolio_id=None) -> dict:
    pm = PortfolioManager()
    listed = pm.list_portfolios()
    requested = (portfolio_id or "").strip() or None
    try:
        status = pm.get_portfolio_status(requested)
    except ValueError:
        status = pm.get_portfolio_status(None)
    positions = status.get("positions") or []
    has_positions = len(positions) > 0
    codes = [p.get("ts_code") for p in positions if p.get("ts_code")]
    price_map = pm._batch_prices(codes) if codes else {}
    today_pnl, today_pct = _today_pnl(positions, price_map) if has_positions else (None, None)

    profile = {}
    try:
        profile = UserProfileManager().get_profile() or {}
    except Exception:
        profile = {}
    sectors = profile.get("favorite_sectors") or []
    news_source, news = _pick_news(positions if has_positions else [], sectors)

    alerts = []
    try:
        raw = AlertHistoryStore().list_items(limit=5)
        for it in raw:
            alerts.append(
                {
                    "id": it.get("id") or "",
                    "message": it.get("message") or "",
                    "triggered_at": it.get("triggered_at") or 0,
                }
            )
    except Exception:
        alerts = []

    snapshot = {
        "portfolio_id": status.get("portfolio_id"),
        "portfolio_name": status.get("portfolio_name"),
        "has_positions": has_positions,
        "total_market_value": None,
        "today_pnl": None,
        "today_pnl_pct": None,
        "total_pnl": None,
        "total_pnl_pct": None,
    }
    if has_positions:
        snapshot["total_market_value"] = status.get("total_market_value")
        snapshot["today_pnl"] = today_pnl
        snapshot["today_pnl_pct"] = today_pct
        snapshot["total_pnl"] = status.get("total_pnl")
        snapshot["total_pnl_pct"] = status.get("total_pnl_pct")

    return {
        "ok": True,
        "portfolios": listed.get("portfolios") or [],
        "active_portfolio_id": listed.get("active_portfolio_id"),
        "snapshot": snapshot,
        "index": _index_snapshot(),
        "news_source": news_source,
        "news": news,
        "alerts": alerts,
    }


def generate_dashboard_comment(portfolio_id, index, news_titles) -> dict:
    raise NotImplementedError("Task 2")
```

先让 `generate_dashboard_comment` 占位会在 Task 2 换成实现。Task 1 **不要**把 `NotImplementedError` 接到 API。本任务 `dashboard.py` 只需要 `build_dashboard_summary`；可以把 `generate_dashboard_comment` 整段删掉，Task 2 再加。

- [ ] **Step 3: 在 `python/api.py` 增加两个路由**

在现有 `@route("GET", "/portfolio/list")` 附近加入：

```python
@route("POST", "/portfolio/active")
def handle_portfolio_active(req):
    pid = (req.body or {}).get("id")
    try:
        PortfolioManager().set_active_portfolio(pid)
    except ValueError as e:
        return {"ok": False, "error": str(e) or "组合不存在"}
    listed = PortfolioManager().list_portfolios()
    return {"ok": True, "active_portfolio_id": listed.get("active_portfolio_id")}


@route("GET", "/dashboard/summary")
def handle_dashboard_summary(req):
    from fin_agent.dashboard import build_dashboard_summary

    return build_dashboard_summary(req.query.get("portfolio_id") or None)
```

文件顶部若尚未 import `PortfolioManager`，沿用该文件现有 import，不要重复。

- [ ] **Step 4: 手动核对**

不编译。确认 `_batch_prices` 含 `pre_close`；summary 无持仓时金额字段为 `None`；无效 `portfolio_id` 不抛 500。

---

### Task 2: 点评 LLM

**Files:**
- Modify: `python/fin-agent/fin_agent/dashboard.py`
- Modify: `python/api.py`

**Interfaces:**
- Consumes: `LLMFactory.create_llm().chat(messages, stream=False)`（同 `news_sentiment.py`）
- Produces: `generate_dashboard_comment(portfolio_id, index, news_titles) -> dict`；`POST /dashboard/comment`

- [ ] **Step 1: 在 `dashboard.py` 增加点评函数**

```python
COMMENT_PROMPT = """你是 A 股投顾助手。根据给定的大盘涨跌和新闻标题，写一句简体中文市场点评。

硬性要求：
- 只输出一句话，不要列表、不要 Markdown、不要引号包裹全文
- 不超过 80 个汉字
- 只使用下面提供的数据，禁止编造未给出的数字或新闻
- 不要荐股、不要给出买卖指令

组合：{portfolio}
指数：{index_line}
新闻：{news_line}
"""


def generate_dashboard_comment(portfolio_id=None, index=None, news_titles=None) -> dict:
    from fin_agent.config import Config
    from fin_agent.llm.factory import LLMFactory

    Config.load()
    titles = [str(t).strip() for t in (news_titles or []) if str(t).strip()]
    news_line = "；".join(titles) if titles else "无"
    index = index or {}
    name = str(index.get("name") or "沪深300")
    if index.get("change_pct") is None:
        index_line = f"{name} 涨跌未知"
    else:
        index_line = f"{name} {float(index.get('change_pct')):+.2f}%"
    portfolio = str(portfolio_id or "当前组合")
    try:
        llm = LLMFactory.create_llm()
        message = llm.chat(
            [
                {
                    "role": "user",
                    "content": COMMENT_PROMPT.format(
                        portfolio=portfolio,
                        index_line=index_line,
                        news_line=news_line,
                    ),
                }
            ],
            stream=False,
        )
        text = (getattr(message, "content", "") or "").strip()
        text = text.replace("```", "").strip().strip('"“”')
        if len(text) > 80:
            text = text[:80].rstrip()
        if not text:
            return {"ok": False, "error": "点评为空"}
        return {"ok": True, "comment": text}
    except ValueError as e:
        return {"ok": False, "error": "未配置模型" if "Unsupported" in str(e) else str(e)}
    except Exception as e:
        return {"ok": False, "error": str(e) or "点评失败"}
```

- [ ] **Step 2: API 路由**

```python
@route("POST", "/dashboard/comment")
def handle_dashboard_comment(req):
    from fin_agent.dashboard import generate_dashboard_comment

    body = req.body or {}
    return generate_dashboard_comment(
        body.get("portfolio_id"),
        body.get("index"),
        body.get("news_titles"),
    )
```

该 POST **不要**放进 `_CSRF_PROTECTED_PREFIXES`（该元组目前只保护 `/news/`）。

- [ ] **Step 3: 手动核对**

确认函数不写 session、不调用 `query_news` 循环。未配置模型时返回 `ok: false` 和中文 `error`。

---

### Task 3: IPC 与类型

**Files:**
- Modify: `src/main/index.ts`（`list-portfolios` 附近）
- Modify: `src/preload/index.ts`
- Modify: `src/renderer/src/env.d.ts`

**Interfaces:**
- Consumes: Task 1–2 HTTP
- Produces:
  - `window.api.getDashboardSummary(portfolioId?: string)`
  - `window.api.setActivePortfolio(id: string)`
  - `window.api.generateDashboardComment(payload)`

- [ ] **Step 1: `env.d.ts` 在 `PortfolioDetail` 后增加类型，并在 `Window.api` 的 `listPortfolios` 旁增加方法**

```ts
interface DashboardIndex {
  ts_code: string
  name: string
  price: number
  change_pct: number
}

interface DashboardNewsItem {
  id: string
  title: string
  source: string
  url: string
  match: 'holding' | 'sector' | 'market'
}

interface DashboardAlertItem {
  id: string
  message: string
  triggered_at: number
}

interface DashboardSnapshot {
  portfolio_id: string
  portfolio_name: string
  has_positions: boolean
  total_market_value: number | null
  today_pnl: number | null
  today_pnl_pct: number | null
  total_pnl: number | null
  total_pnl_pct: number | null
}

interface DashboardSummary {
  ok: boolean
  portfolios: PortfolioMeta[]
  active_portfolio_id: string
  snapshot: DashboardSnapshot
  index: DashboardIndex | null
  news_source: 'holding' | 'sector' | 'market'
  news: DashboardNewsItem[]
  alerts: DashboardAlertItem[]
  error?: string
}
```

`Window.api`：

```ts
    getDashboardSummary: (portfolioId?: string) => Promise<DashboardSummary>
    setActivePortfolio: (id: string) => Promise<{ ok: boolean; active_portfolio_id?: string; error?: string }>
    generateDashboardComment: (payload: {
      portfolio_id?: string
      index?: { name?: string; change_pct?: number | null }
      news_titles?: string[]
    }) => Promise<{ ok: boolean; comment?: string; error?: string }>
```

- [ ] **Step 2: preload `listPortfolios` 旁**

```ts
  getDashboardSummary: (portfolioId?: string) =>
    ipcRenderer.invoke('get-dashboard-summary', portfolioId),
  setActivePortfolio: (id: string) => ipcRenderer.invoke('set-active-portfolio', id),
  generateDashboardComment: (payload: object) =>
    ipcRenderer.invoke('generate-dashboard-comment', payload),
```

- [ ] **Step 3: `src/main/index.ts` 在 `list-portfolios` 后**

```ts
  ipcMain.handle('get-dashboard-summary', async (_e, portfolioId?: string) => {
    const q = portfolioId ? `?portfolio_id=${encodeURIComponent(portfolioId)}` : ''
    return makeApiRequest(`/dashboard/summary${q}`)
  })

  ipcMain.handle('set-active-portfolio', async (_e, id: string) =>
    makeApiRequest('/portfolio/active', 'POST', { id })
  )

  ipcMain.handle('generate-dashboard-comment', async (_e, payload: unknown) =>
    makeApiRequest('/dashboard/comment', 'POST', payload)
  )
```

- [ ] **Step 4: 手动核对**

三处名字一致。不要改 mica / onboarding IPC。

---

### Task 4: 空会话 UI

**Files:**
- Modify: `src/renderer/src/components/StockSearchModal.tsx`
- Modify: `src/renderer/src/utils/chatPrefill.ts`
- Create: `src/renderer/src/components/DashboardWelcome.tsx`
- Modify: `src/renderer/src/components/ChatView.tsx`
- Modify: `src/renderer/src/index.css`

**Interfaces:**
- Consumes: Task 3 的三个 `window.api`；`requestPrefill`；`buildAnalyzeStockPrefill` / `buildPortfolioDiagnosePrefill`
- Produces: 空会话四块仪表盘

- [ ] **Step 1: `StockSearchModal` 增加可选 `onPick`**

```ts
export interface StockSearchModalProps {
  open: boolean
  onClose: () => void
  onPick?: (item: StockSearchItem) => void
}
```

`openStock` 改为：

```ts
  const openStock = (item: StockSearchItem) => {
    onClose()
    if (onPick) {
      onPick(item)
      return
    }
    navigate(`/stock/${encodeURIComponent(item.ts_code)}`)
  }
```

列表点击从 `openStock(item.ts_code)` 改为 `openStock(item)`。顶栏现有用法不传 `onPick`，行为不变。

- [ ] **Step 2: `chatPrefill.ts` 增加**

```ts
export function buildWatchTodayPrefill(input: {
  portfolioName?: string
  indexName?: string
  changePct?: number | null
  newsTitles?: string[]
}): string {
  const name = input.portfolioName || '当前组合'
  let indexLine = '大盘数据暂缺'
  if (input.indexName && input.changePct != null && Number.isFinite(input.changePct)) {
    const n = input.changePct
    indexLine = `${input.indexName} ${n > 0 ? '+' : ''}${n.toFixed(2)}%`
  }
  const news = (input.newsTitles || []).filter(Boolean).slice(0, 3)
  const newsLine = news.length ? news.map((t, i) => `${i + 1}. ${t}`).join('；') : '暂无新闻'
  return (
    `请根据今天的市场与资讯，告诉我「今日该关注什么」。` +
    `组合：${name}。大盘：${indexLine}。新闻：${newsLine}。` +
    `按结论→依据→风险→下一步回答，不要编造未提供的数据。`
  )
}
```

- [ ] **Step 3: 新建 `DashboardWelcome.tsx`**

要点（实现时按此结构写完整组件，不要漏块）：

- `useEffect` 在 mount 调 `window.api.getDashboardSummary()`；失败设 `loadError`
- 顶部失败条：「仪表盘加载失败，可直接在下方提问」+ 按钮「重试」
- 总览：`FaSelect` 选项为 `portfolios`；`onChange` 先 `setActivePortfolio` 再重新 summary；失败 `alert` 且不改当前 id
- 无持仓：文案「还没有持仓」+「去添加」`navigate('/portfolio')`
- 有持仓：三列数字；`toneClass`/`signed`/`money` 与 `PortfolioView.tsx` 相同（红涨绿跌）。点击数字区 `navigate('/portfolio')`
- 简报：指数行或「大盘暂不可用」；新闻列表点击：`openExternal(url)`；若 `id` 非空则 `markNewsRead(id, true)`
- 「生成点评」调用 `generateDashboardComment`；`commenting` 时 disabled；失败展示「点评暂不可用」
- 待关注：最多 5 条，点击 `onOpenReminders()`；无则「暂无预警」；固定「投资日历即将推出」
- 三条入口：分析打开本组件内 `StockSearchModal` 且 `onPick` → `requestPrefill(buildAnalyzeStockPrefill(ts_code, name))`；体检无持仓 `alert({ title: '请先添加持仓' })`，有则 diagnose prefill；今日关注 `buildWatchTodayPrefill`
- props：`onOpenReminders: () => void`
- 不要自己再挂一套 `ReminderTasksModal`（用 ChatView 已有的）

数字格式从 `PortfolioView` 复制这三行即可，不要 import PortfolioView：

```ts
const money = (n: number) => n.toLocaleString('zh-CN', { maximumFractionDigits: 2 })
const toneClass = (n: number) =>
  n > 0 ? 'text-red-400' : n < 0 ? 'text-emerald-400' : 'text-[var(--fa-muted)]'
const signed = (n: number) => `${n > 0 ? '+' : ''}${money(n)}`
```

卡片容器 class：`fa-card p-4`。外层：`fa-dashboard-welcome no-drag`（Step 5 的 CSS）。

- [ ] **Step 4: 改 `ChatView.tsx`**

1. import `DashboardWelcome`。
2. `quickReplyOptions`：`showWelcomeHero` 时返回 `[]`，不要 `getDefaultQuickReplyOptions()`。
3. 将

```tsx
        {showWelcomeHero ? (
          <div className="fa-hero no-drag">
            ...
          </div>
        ) : (
```

换成：

```tsx
        {showWelcomeHero ? (
          <DashboardWelcome onOpenReminders={() => setReminderModalOpen(true)} />
        ) : (
```

不要删除顶栏的 `StockSearchModal`（无 `onPick`，仍进个股页）。`ReminderTasksModal` 仍由 ChatView 持有。

- [ ] **Step 5: `index.css` 在 `.fa-hero` 前插入**

```css
  .fa-dashboard-welcome {
    @apply min-h-0 flex-1 overflow-y-auto px-4 py-6 md:px-8;
  }

  .fa-dashboard-welcome-inner {
    @apply mx-auto flex w-full max-w-3xl flex-col gap-4 pb-28;
  }

  .fa-dashboard-metrics {
    @apply grid grid-cols-3 gap-3;
  }

  .fa-dashboard-actions {
    @apply grid grid-cols-1 gap-2 sm:grid-cols-3;
  }
```

- [ ] **Step 6: 手动核对（本任务）**

对照 spec §9。不编译。确认空会话不再出现「今天想分析什么？」和欢迎快捷语句。

---

## Spec coverage

| Spec | Task |
|------|------|
| §4 空会话出现 / 发消息消失 | Task 4 ChatView |
| §5.1 总览、切换组合、无持仓 CTA | Task 1 active + Task 4 |
| §5.2 指数、新闻、点击生成点评 | Task 1–2 + Task 4 |
| §5.3 预警 + 日历占位 | Task 1 alerts + Task 4 |
| §5.4 三条入口 | Task 4 |
| §6.1 summary 字段与今日盈亏规则 | Task 1 |
| §6.2 POST /portfolio/active | Task 1 |
| §6.3 comment 不写 session | Task 2 |
| §8 失败不挡输入框 | Task 4（ChatView 输入岛仍在仪表盘下方） |
| 新闻本地 id 才标已读 | Task 1 直播新闻 `id: ""` + Task 4 |
| 不按经验分层 / 无独立路由 | 全任务不实现 |

## Self-review notes

- 未使用 TDD：与仓库约束一致。
- `_batch_prices` 必须带 `pre_close`，否则今日盈亏无法按 spec 计算。
- 欢迎快捷语句必须在 `showWelcomeHero` 时返回空数组，否则会与场景入口重复。
