# v1.1 结构化分析报告 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 个股体检、组合诊断、买卖决策备忘录在聊天里以固定报告卡片出现；深度跟 `experience_level`；卡片可收藏完整副本。

**Architecture:** 模型在正文末尾附 `FIN_AGENT_REPORT_JSON`。Python 从内容流中剥掉该标记，校验通过后 `yield {"type": "report", "report": {...}}`。ChatView 把该事件写成助手消息的 `report` 块并用 `ReportCard` 渲染。收藏写入配置目录 `analysis_favorites.json`。

**Tech Stack:** Electron IPC、React + TypeScript、`python/api.py`、`agent/core.py`、现有 `fa-card` / ReactMarkdown。

**Spec:** `docs/superpowers/specs/2026-08-21-v1.1-structured-report-design.md`

## Global Constraints

- 三种报告本轮都做；自由提问只要判定是这三类分析，一律出报告卡；闲聊、查行情仍是普通 Markdown。
- 「该不该买/卖、能不能上车、止损设哪」→ `trade_memo`；一般「分析这只股票」→ `stock_checkup`；组合诊断/体检 → `portfolio_diagnose`。
- 共用章节：结论 → 依据 → 风险 → 下一步。深度只跟画像，卡片上不可切简版/完整。
- `beginner`→`brief`（简版），`experienced`→`full`（完整），`Unknown`→`standard`（适中）。
- 实现路径：`FIN_AGENT_REPORT_JSON` + Python 抽出 `report` 事件；前端不必再扫正文。
- 收藏：完整 JSON 副本写入 `analysis_favorites.json`；删会话后仍在；成功后「已收藏」，再点取消。
- 研报夹列表 / Watchlist / 独立 `/reports` 页面本轮不做。
- `FIN_AGENT_CHOICES_JSON` 仍可用，与报告标记互不替代。
- 面向用户文案用简体中文。
- 不生成 Mock；不编译项目；不写自动化测试；实现后由用户按 spec §8 手工验收。
- 未经用户明确要求不要 git commit。
- `POST /reports/favorites` **不要**放进 `_CSRF_PROTECTED_PREFIXES`。

---

## File Map

| 文件 | 职责 |
|------|------|
| `python/fin-agent/fin_agent/report_parse.py` | **新建** 抽出/校验 JSON、流式过滤标记 |
| `python/fin-agent/fin_agent/agent/core.py` | prompt + 内容流过滤 + `yield report` |
| `python/fin-agent/fin_agent/analysis_favorites.py` | **新建** 收藏 JSON 存储 |
| `python/api.py` | `POST/DELETE /reports/favorites` |
| `src/main/index.ts` | IPC |
| `src/preload/index.ts` / `src/renderer/src/env.d.ts` | `window.api` |
| `src/renderer/src/contexts/ChatContext.tsx` | `ChatBlock` 增加 `report` |
| `src/renderer/src/components/ReportCard.tsx` | **新建** 报告卡 + 收藏 |
| `src/renderer/src/components/ChatView.tsx` | 处理 `report` 事件并渲染 |
| `src/renderer/src/utils/chatPrefill.ts` | 预填点明 `kind` |
| `src/renderer/src/index.css` | 报告卡样式 |

---

### Task 1: 解析与校验 `FIN_AGENT_REPORT_JSON`

**Files:**
- Create: `python/fin-agent/fin_agent/report_parse.py`

**Interfaces:**
- Consumes: 助手完整正文（str）
- Produces:
  - `REPORT_MARKER = "FIN_AGENT_REPORT_JSON"`
  - `DISCLAIMER = "以上内容仅供参考，不构成投资建议。"`
  - `extract_report(text: str) -> tuple[str, dict | None]`（清洗后正文, 合法 payload 或 None）
  - `ReportStreamFilter`：`feed(chunk) -> str`、`flush() -> str`

- [ ] **Step 1: 新建 `report_parse.py`**

全文如下（不要 Mock；不要拆到别的模块）：

```python
from __future__ import annotations

import json

REPORT_MARKER = "FIN_AGENT_REPORT_JSON"
DISCLAIMER = "以上内容仅供参考，不构成投资建议。"
KINDS = ("stock_checkup", "portfolio_diagnose", "trade_memo")
DEPTHS = ("brief", "standard", "full")
SECTION_KEYS = ("conclusion", "evidence", "risk", "next")


def _skip_ws(text: str, i: int) -> int:
    n = len(text)
    while i < n and text[i] in " \t\r\n":
        i += 1
    return i


def _parse_json_value(text: str, start: int):
    """从 start 解析一个 JSON 值，返回 (obj, end_index)。失败返回 (None, start)。"""
    i = _skip_ws(text, start)
    if i >= len(text):
        return None, start
    try:
        obj, end = json.JSONDecoder().raw_decode(text, i)
    except json.JSONDecodeError:
        return None, start
    return obj, end


def validate_report(obj) -> dict | None:
    if not isinstance(obj, dict):
        return None
    kind = obj.get("kind")
    title = str(obj.get("title") or "").strip()
    if kind not in KINDS or not title:
        return None
    sections_in = obj.get("sections")
    if not isinstance(sections_in, dict):
        return None
    sections = {}
    for key in SECTION_KEYS:
        val = str(sections_in.get(key) or "").strip()
        if not val:
            return None
        sections[key] = val
    depth = obj.get("depth")
    if depth not in DEPTHS:
        depth = "standard"
    symbols = obj.get("symbols") or []
    if not isinstance(symbols, list):
        symbols = []
    symbols = [str(s).strip() for s in symbols if str(s).strip()]
    pid = obj.get("portfolio_id")
    if pid is not None:
        pid = str(pid).strip() or None
    disclaimer = str(obj.get("disclaimer") or "").strip() or DISCLAIMER
    return {
        "kind": kind,
        "title": title,
        "depth": depth,
        "symbols": symbols,
        "portfolio_id": pid,
        "sections": sections,
        "disclaimer": disclaimer,
    }


def extract_report(text: str) -> tuple[str, dict | None]:
    raw = text or ""
    idx = raw.rfind(REPORT_MARKER)
    if idx < 0:
        return raw, None
    obj, end = _parse_json_value(raw, idx + len(REPORT_MARKER))
    payload = validate_report(obj)
    if payload is None:
        return raw, None
    before = raw[:idx].rstrip()
    after = raw[end:].lstrip()
    cleaned = before if not after else f"{before}\n{after}"
    return cleaned.rstrip(), payload


class ReportStreamFilter:
    """从流式 content 中扣掉 FIN_AGENT_REPORT_JSON 及其 JSON 对象，其余（含 CHOICES）原样放出。"""

    def __init__(self):
        self._buf = ""
        self._in_json = False

    def feed(self, chunk: str) -> str:
        if not chunk:
            return ""
        self._buf += chunk
        out = []
        while self._buf:
            if not self._in_json:
                idx = self._buf.find(REPORT_MARKER)
                if idx == -1:
                    hold = 0
                    max_tail = min(len(REPORT_MARKER) - 1, len(self._buf))
                    for length in range(max_tail, 0, -1):
                        if REPORT_MARKER.startswith(self._buf[-length:]):
                            hold = length
                            break
                    if hold == 0:
                        out.append(self._buf)
                        self._buf = ""
                    else:
                        out.append(self._buf[:-hold])
                        self._buf = self._buf[-hold:]
                    break
                out.append(self._buf[:idx])
                self._buf = self._buf[idx + len(REPORT_MARKER):]
                self._in_json = True
                continue
            obj, end = _parse_json_value(self._buf, 0)
            if obj is None:
                break
            self._buf = self._buf[end:]
            self._in_json = False
        return "".join(out)

    def flush(self) -> str:
        if self._in_json:
            self._buf = ""
            self._in_json = False
            return ""
        out = self._buf
        self._buf = ""
        return out
```

- [ ] **Step 2: 手动核对**

不编译、不写测试。在脑中过一遍：

- 正文 + 标记 + JSON + `FIN_AGENT_CHOICES_JSON [...]` → `extract_report` 返回 CHOICES 行仍在、payload 合法
- JSON 缺 `sections.risk` → payload 为 None，原文不动
- `kind` 非法 → None
- `disclaimer` 缺省 → 固定免责句

---

### Task 2: Agent prompt + 流式抽出 report 事件

**Files:**
- Modify: `python/fin-agent/fin_agent/agent/core.py`

**Interfaces:**
- Consumes: Task 1 的 `extract_report`、`ReportStreamFilter`、`REPORT_MARKER`
- Produces: `stream_chat` 在最终无 tool_calls 时，若 payload 合法则 `yield {"type": "report", "report": payload}`；content 流不含报告标记行

- [ ] **Step 1: 在 `_get_system_content` 的「经验分层」段落后、「快捷回复」段落前插入**

```python
            "### 结构化报告 FIN_AGENT_REPORT_JSON ###\n"
            "下列三类意图的最终回复必须在全文末尾单独一行输出标记"
            "（不要放进代码围栏），紧跟一个 JSON 对象：\n"
            "- stock_checkup：分析/体检某只股票、个股基本面技术面。"
            "「依据」须覆盖基本面、技术面、估值；完整版加指标表。\n"
            "- portfolio_diagnose：组合诊断/体检、持仓健不健康。"
            "「依据」须覆盖行业分布、集中度、盈亏归因、与风险偏好是否匹配；"
            "「下一步」给调仓建议。\n"
            "- trade_memo：该不该买/卖、能不能上车、仓位与止损。"
            "「依据」须覆盖看多或看空理由、支撑/压力位；"
            "「下一步」给建议仓位与止损参考。\n"
            "JSON 字段：kind、title、depth、symbols、portfolio_id、"
            "sections.conclusion/evidence/risk/next、disclaimer。\n"
            "depth 必须按当前画像：beginner→brief，experienced→full，Unknown→standard。"
            "章节标题仍是结论/依据/风险/下一步，不要另起一套一级标题。\n"
            "问候、查现价、设提醒、纯工具确认、新闻闲聊禁止输出该标记。\n"
            f"disclaimer 固定为：{DISCLAIMER}\n"
            "该行之后仍可追加 FIN_AGENT_CHOICES_JSON。\n\n"
```

文件顶部（`_get_system_content` 所在类能引用到）增加：

```python
from fin_agent.report_parse import DISCLAIMER, ReportStreamFilter, extract_report
```

`stream_chat` 的 docstring Types 列表补上 `'report'`。

- [ ] **Step 2: 在流式循环里过滤 content**

在 `full_content = ""` 之后加入：

```python
                        report_filter = ReportStreamFilter()

                        def emit_content(text):
                            if not text:
                                return
                            visible = report_filter.feed(text)
                            if visible:
                                yield {"type": "content", "content": visible}
```

把该 `if stream_mode and inspect.isgenerator(response):` 分支里所有

`yield {"type": "content", "content": ...}`

改成：

```python
                                            for ev in emit_content(...):
                                                yield ev
```

`...` 保持原来的表达式（`pre` / `buffer` / `buffer[:idx]` / `"<"` 等）。**不要**过滤 `thinking`。

非流式兜底：

```python
                        if message.content:
                            for ev in emit_content(message.content):
                                yield ev
```

这里非流式分支没有 `report_filter`。在 `stream_chat` 方法开头（`full_content` 之前、LLM 校验之后）建一个贯穿整个 `while True` 的 filter：

```python
        report_filter = ReportStreamFilter()

        def emit_content(text):
            if not text:
                return
            visible = report_filter.feed(text)
            if visible:
                yield {"type": "content", "content": visible}
```

把它放在 `step = 0` 附近，流式/非流式共用。删掉循环内部重复定义。

- [ ] **Step 3: 最终 answer 时 yield report**

把

```python
                if not message.tool_calls:
                    answer = message.content if message.content else ""
                    self.history.append(self._to_dict(message))
                    yield {"type": "answer", "content": answer}
                    return
```

改成：

```python
                if not message.tool_calls:
                    answer = message.content if message.content else ""
                    leftover = report_filter.flush()
                    if leftover:
                        yield {"type": "content", "content": leftover}
                    cleaned, report = extract_report(answer)
                    self.history.append(self._to_dict(message))
                    if report:
                        yield {"type": "report", "report": report}
                    yield {"type": "answer", "content": cleaned}
                    return
```

`history` 仍存模型原文（含标记），不要改 session LLM 历史。

- [ ] **Step 4: 手动核对**

不编译。确认：content 的 yield 都走 `emit_content`；无 tool_calls 时先 `report` 再 `answer`；CHOICES 标记仍会出现在 content/answer 里。

---

### Task 3: 收藏存储与 HTTP

**Files:**
- Create: `python/fin-agent/fin_agent/analysis_favorites.py`
- Modify: `python/api.py`

**Interfaces:**
- Consumes: Task 1 `validate_report`（写入前再校验一遍）
- Produces:
  - `AnalysisFavoritesStore.add(payload, source_session_id=None) -> dict`
  - `AnalysisFavoritesStore.remove(item_id) -> bool`
  - `POST /reports/favorites` → `{ ok: true, id }` 或 `{ ok: false, error }`
  - `DELETE /reports/favorites` body `{ "id" }` → `{ ok: true }` 或 `{ ok: false, error }`

- [ ] **Step 1: 新建 `analysis_favorites.py`**

模式对齐 `alert_history.py`（RLock、config 目录、JSON 文件）：

```python
from __future__ import annotations

import json
import os
import threading
import time
import uuid

from fin_agent.config import Config
from fin_agent.report_parse import DISCLAIMER, validate_report

_LOCK = threading.RLock()
_MAX_ITEMS = 500


class AnalysisFavoritesStore:
    def __init__(self, path=None):
        if path:
            self.path = path
        else:
            config_dir = Config.get_config_dir()
            os.makedirs(config_dir, exist_ok=True)
            self.path = os.path.join(config_dir, "analysis_favorites.json")

    def _load(self):
        if not os.path.exists(self.path):
            return {"items": []}
        try:
            with open(self.path, "r", encoding="utf-8") as handle:
                data = json.load(handle)
        except Exception:
            return {"items": []}
        if not isinstance(data, dict) or not isinstance(data.get("items"), list):
            return {"items": []}
        return data

    def _save(self, data):
        directory = os.path.dirname(self.path)
        if directory:
            os.makedirs(directory, exist_ok=True)
        with open(self.path, "w", encoding="utf-8") as handle:
            json.dump(data, handle, indent=2, ensure_ascii=False)

    def add(self, payload: dict, source_session_id=None) -> dict:
        report = validate_report(payload)
        if report is None:
            raise ValueError("报告内容不完整")
        record = {
            "id": uuid.uuid4().hex,
            "created_at": int(time.time()),
            **report,
            "disclaimer": report.get("disclaimer") or DISCLAIMER,
            "source_session_id": (str(source_session_id).strip() if source_session_id else None),
        }
        with _LOCK:
            data = self._load()
            data["items"].insert(0, record)
            data["items"] = data["items"][:_MAX_ITEMS]
            self._save(data)
        return record

    def remove(self, item_id: str) -> bool:
        target = str(item_id or "").strip()
        if not target:
            return False
        with _LOCK:
            data = self._load()
            items = data.get("items") or []
            kept = [it for it in items if str(it.get("id") or "") != target]
            if len(kept) == len(items):
                return False
            data["items"] = kept
            self._save(data)
            return True
```

- [ ] **Step 2: `python/api.py` 增加路由**

放在 `POST /dashboard/comment` 附近。**不要**把 `/reports/` 加入 `_CSRF_PROTECTED_PREFIXES`（保持 `("/news/",)`）。

```python
@route("POST", "/reports/favorites")
def handle_report_favorite_add(req):
    from fin_agent.analysis_favorites import AnalysisFavoritesStore

    body = req.body or {}
    try:
        record = AnalysisFavoritesStore().add(body, body.get("source_session_id"))
    except ValueError as e:
        return {"ok": False, "error": str(e) or "收藏失败"}
    except Exception:
        return {"ok": False, "error": "收藏失败"}
    return {"ok": True, "id": record["id"]}


@route("DELETE", "/reports/favorites")
def handle_report_favorite_delete(req):
    from fin_agent.analysis_favorites import AnalysisFavoritesStore

    body = req.body or {}
    ok = AnalysisFavoritesStore().remove((body or {}).get("id"))
    if not ok:
        return {"ok": False, "error": "未找到该收藏"}
    return {"ok": True}
```

`add` 把整个 body 丢给 `validate_report`：前端应传报告字段（kind/title/depth/symbols/portfolio_id/sections/disclaimer），`source_session_id` 为额外键，`validate_report` 会忽略未知字段。

- [ ] **Step 3: 手动核对**

确认 CSRF 元组未改；无效 payload 返回中文 `error`；文件路径是 config 目录下 `analysis_favorites.json`。

---

### Task 4: IPC、类型、ChatBlock

**Files:**
- Modify: `src/main/index.ts`（`generate-dashboard-comment` 附近）
- Modify: `src/preload/index.ts`
- Modify: `src/renderer/src/env.d.ts`
- Modify: `src/renderer/src/contexts/ChatContext.tsx`（`ChatBlock` 联合类型）

**Interfaces:**
- Consumes: Task 3 HTTP
- Produces:
  - `window.api.saveAnalysisFavorite(payload)`
  - `window.api.deleteAnalysisFavorite(id: string)`
  - `ChatBlock` 的 `type: 'report'` 变体

- [ ] **Step 1: `env.d.ts` 在 `DashboardSummary` 后增加类型，并在 `Window.api` 的 `generateDashboardComment` 旁加方法**

```ts
type AnalysisReportKind = 'stock_checkup' | 'portfolio_diagnose' | 'trade_memo'
type AnalysisReportDepth = 'brief' | 'standard' | 'full'

interface AnalysisReportSections {
  conclusion: string
  evidence: string
  risk: string
  next: string
}

interface AnalysisReportPayload {
  kind: AnalysisReportKind
  title: string
  depth: AnalysisReportDepth
  symbols: string[]
  portfolio_id?: string | null
  sections: AnalysisReportSections
  disclaimer: string
  favorite_id?: string
}
```

```ts
    saveAnalysisFavorite: (payload: AnalysisReportPayload & { source_session_id?: string }) =>
      Promise<{ ok: boolean; id?: string; error?: string }>
    deleteAnalysisFavorite: (id: string) => Promise<{ ok: boolean; error?: string }>
```

- [ ] **Step 2: preload，紧挨 `generateDashboardComment`**

```ts
  saveAnalysisFavorite: (payload: object) => ipcRenderer.invoke('save-analysis-favorite', payload),
  deleteAnalysisFavorite: (id: string) => ipcRenderer.invoke('delete-analysis-favorite', id),
```

- [ ] **Step 3: `src/main/index.ts`，紧挨 `generate-dashboard-comment`**

```ts
  ipcMain.handle('save-analysis-favorite', async (_e, payload: unknown) =>
    makeApiRequest('/reports/favorites', 'POST', payload)
  )

  ipcMain.handle('delete-analysis-favorite', async (_e, id: string) =>
    makeApiRequest('/reports/favorites', 'DELETE', { id })
  )
```

不要改 mica / onboarding / dashboard 已有 IPC。

- [ ] **Step 4: `ChatContext.tsx` 的 `ChatBlock` 增加**

```ts
  | {
      type: 'report'
      kind: 'stock_checkup' | 'portfolio_diagnose' | 'trade_memo'
      title: string
      depth: 'brief' | 'standard' | 'full'
      symbols: string[]
      portfolio_id?: string | null
      sections: {
        conclusion: string
        evidence: string
        risk: string
        next: string
      }
      disclaimer: string
      favorite_id?: string
    }
```

- [ ] **Step 5: 手动核对**

三处 channel / JS 名一致：`save-analysis-favorite` ↔ `saveAnalysisFavorite`，`delete-analysis-favorite` ↔ `deleteAnalysisFavorite`。

---

### Task 5: 报告卡片 UI + 预填 + 流式接入

**Files:**
- Create: `src/renderer/src/components/ReportCard.tsx`
- Modify: `src/renderer/src/components/ChatView.tsx`
- Modify: `src/renderer/src/utils/chatPrefill.ts`
- Modify: `src/renderer/src/index.css`

**Interfaces:**
- Consumes: Task 2 的 `report` 事件；Task 4 的 `ChatBlock` / `saveAnalysisFavorite`
- Produces: 助手消息里的报告卡；收藏写回 `favorite_id`

- [ ] **Step 1: 预填点明 kind**

`buildAnalyzeStockPrefill` / `buildPortfolioDiagnosePrefill` 末尾补一句（保留原句，只追加）：

```ts
export function buildAnalyzeStockPrefill(tsCode: string, name?: string): string {
  const label = name ? `${name}（${tsCode}）` : tsCode
  return (
    `请按「结论 → 依据 → 风险 → 下一步」结构，对 ${label} 做个股体检分析。` +
    `这是个股体检，回复末尾必须附 FIN_AGENT_REPORT_JSON，kind=stock_checkup。`
  )
}

export function buildPortfolioDiagnosePrefill(portfolioId: string, portfolioName?: string): string {
  const label = portfolioName ? `「${portfolioName}」` : ''
  return (
    `请诊断当前组合${label}（id=${portfolioId}）：按结论→依据→风险→下一步给出健康度与调仓建议；先调用持仓工具获取真实数据。` +
    `这是组合诊断，回复末尾必须附 FIN_AGENT_REPORT_JSON，kind=portfolio_diagnose。`
  )
}

export function buildTradeMemoPrefill(tsCode: string, name?: string): string {
  const label = name ? `${name}（${tsCode}）` : tsCode
  return (
    `请就 ${label} 写一份买卖决策备忘录：看多或看空理由、关键支撑/压力位、建议仓位与止损参考。` +
    `按结论→依据→风险→下一步回答。这是买卖备忘录，回复末尾必须附 FIN_AGENT_REPORT_JSON，kind=trade_memo。`
  )
}
```

本轮不在仪表盘加第四个按钮；`buildTradeMemoPrefill` 供后续或手测复用即可。

- [ ] **Step 2: 新建 `ReportCard.tsx`**

```tsx
import React, { useState } from 'react'
import ReactMarkdown from 'react-markdown'
import remarkGfm from 'remark-gfm'
import { Bookmark } from 'lucide-react'
import { useAppDialog } from '../contexts/AppDialogContext'
import { MarkdownExternalLink } from './ExternalLink'

const KIND_LABEL: Record<ChatBlockReport['kind'], string> = {
  stock_checkup: '个股体检',
  portfolio_diagnose: '组合诊断',
  trade_memo: '买卖决策备忘录'
}

const DEPTH_LABEL: Record<ChatBlockReport['depth'], string> = {
  brief: '简版',
  standard: '适中',
  full: '完整'
}

type ChatBlockReport = Extract<ChatBlock, { type: 'report' }>

const markdownComponents = {
  a: MarkdownExternalLink
}

export interface ReportCardProps {
  block: ChatBlockReport
  sessionId: string | null
  onFavoriteId: (id: string | undefined) => void
}

const ReportCard: React.FC<ReportCardProps> = ({ block, sessionId, onFavoriteId }) => {
  const { alert } = useAppDialog()
  const [busy, setBusy] = useState(false)
  const saved = Boolean(block.favorite_id)

  const toggleFavorite = async () => {
    if (busy) return
    setBusy(true)
    try {
      if (saved && block.favorite_id) {
        const res = await window.api.deleteAnalysisFavorite(block.favorite_id)
        if (!res?.ok) {
          await alert({ title: res?.error || '取消收藏失败' })
          return
        }
        onFavoriteId(undefined)
      } else {
        const res = await window.api.saveAnalysisFavorite({
          kind: block.kind,
          title: block.title,
          depth: block.depth,
          symbols: block.symbols || [],
          portfolio_id: block.portfolio_id ?? null,
          sections: block.sections,
          disclaimer: block.disclaimer,
          source_session_id: sessionId || undefined
        })
        if (!res?.ok || !res.id) {
          await alert({ title: res?.error || '收藏失败' })
          return
        }
        onFavoriteId(res.id)
      }
    } catch (e) {
      await alert({ title: e instanceof Error ? e.message : saved ? '取消收藏失败' : '收藏失败' })
    } finally {
      setBusy(false)
    }
  }

  const section = (title: string, body: string) => (
    <div className="mt-3">
      <h3 className="mb-1 text-sm font-semibold">{title}</h3>
      <div className="prose prose-fa prose-sm max-w-none">
        <ReactMarkdown remarkPlugins={[remarkGfm]} components={markdownComponents}>
          {body}
        </ReactMarkdown>
      </div>
    </div>
  )

  return (
    <div className="fa-report-card">
      <div className="flex flex-wrap items-center gap-2">
        <span className="fa-report-kind">{KIND_LABEL[block.kind]}</span>
        <span className="fa-report-depth">{DEPTH_LABEL[block.depth]}</span>
        <button
          type="button"
          className="fa-btn-ghost ml-auto px-2 py-1 text-xs"
          disabled={busy}
          onClick={() => void toggleFavorite()}
        >
          <Bookmark size={14} className="mr-1 inline" />
          {saved ? '已收藏' : '收藏'}
        </button>
      </div>
      <h2 className="mt-2 text-base font-semibold">{block.title}</h2>
      {section('结论', block.sections.conclusion)}
      {section('依据', block.sections.evidence)}
      {section('风险', block.sections.risk)}
      {section('下一步', block.sections.next)}
      <p className="mt-4 text-[11px] text-[var(--fa-faint)]">{block.disclaimer}</p>
    </div>
  )
}

export default ReportCard
```

`ChatBlock` 从 `../contexts/ChatContext` import（加到 import 列表，不要在文件里重复声明 `ChatBlock` 后再从别处引导致冲突）。把上面的 `type ChatBlockReport` 放到 import 之后：

```tsx
import type { ChatBlock } from '../contexts/ChatContext'
```

删除组件内重复的 `ChatBlockReport` 前向引用问题：先 import `ChatBlock`，再写 `type ChatBlockReport = Extract<...>`，再写 `KIND_LABEL`。

失败时按钮文案不变（`saved` 仍看 `block.favorite_id`）。

- [ ] **Step 3: `index.css` 在 `.fa-news-chat-card` 前插入**

```css
  .fa-report-card {
    @apply w-full rounded-2xl border px-4 py-4 text-left;
    border-color: var(--fa-border);
    background: var(--fa-surface);
  }

  .fa-report-kind {
    @apply inline-flex shrink-0 items-center rounded-full px-2 py-0.5 text-[10px] font-medium;
    color: var(--fa-accent);
    background: var(--fa-accent-soft);
  }

  .fa-report-depth {
    @apply inline-flex shrink-0 items-center rounded-full px-2 py-0.5 text-[10px];
    color: var(--fa-muted);
    border: 1px solid var(--fa-border-subtle);
  }
```

不要删 onboarding / dashboard / mica 样式。

- [ ] **Step 4: `ChatView.tsx` 接入**

1. `import ReportCard from './ReportCard'`
2. 在 `markResponding` 那组 type 判断里**不要**把 `report` 当成 `error`/`finish`。`report` 出现在 `answer` 之前或之后都可以；收到 `report` 时会话仍可保持 responding，直到现有 `finish`。
3. 在 `patchFromStream` 的 `data.type === 'finish'` 之前增加：

```ts
            } else if (data.type === 'report' && data.report && typeof data.report === 'object') {
                const r = data.report as AnalysisReportPayload
                if (r.kind && r.title && r.sections) {
                    assistantMsg.blocks.push({
                        type: 'report',
                        kind: r.kind,
                        title: r.title,
                        depth: r.depth || 'standard',
                        symbols: Array.isArray(r.symbols) ? r.symbols : [],
                        portfolio_id: r.portfolio_id ?? null,
                        sections: r.sections,
                        disclaimer: r.disclaimer || '以上内容仅供参考，不构成投资建议。'
                    })
                }
            } else if (data.type === 'error') {
```

4. 渲染：在 `tool_execution` 与 `text` 分支之间插入 `report`。若该条消息已有 `report` 块，**跳过所有 `text` 块**（避免和卡片重复）；`thinking` / `tool_execution` 仍显示。快捷选项继续用现有 `getQuickReplyOptions(messages, …)`（它读 text 块里残留的 CHOICES）。

```tsx
                        if (block.type === 'report') {
                          return (
                            <ReportCard
                              key={bIdx}
                              block={block}
                              sessionId={activeSessionId}
                              onFavoriteId={(id) => {
                                setMessages((prev) => {
                                  const next = [...prev]
                                  const msg = next[idx]
                                  if (!msg?.blocks) return prev
                                  next[idx] = {
                                    ...msg,
                                    blocks: msg.blocks.map((b, i) =>
                                      i === bIdx && b.type === 'report'
                                        ? { ...b, favorite_id: id }
                                        : b
                                    )
                                  }
                                  return next
                                })
                              }}
                            />
                          )
                        }
                        if (block.type === 'text') {
                          if ((msg.blocks || []).some((b) => b.type === 'report')) return null
```

`setMessages` 必须是 ChatView 里已有的更新当前会话消息的 API（现有 `useChat()` 的 `setMessages`）。不要新建一套会话。

5. `assistantHasStarted`：`report` 块视为已开始输出。

```ts
    if (block.type === 'report') return true
```

- [ ] **Step 5: 手动核对（对照 spec §8）**

不编译。确认：

- 有 `report` 时不渲染分析 Markdown 正文，工具卡仍在
- 无 `report` 时行为与现网一致
- 顶栏/仪表盘搜索预填已带 kind 句
- 收藏失败走 `alert`，按钮仍显示「收藏」

---

## Spec coverage

| Spec | Task |
|------|------|
| §4 三类意图出卡 / 闲聊不出 | Task 2 prompt |
| §5.1 卡片外壳与四章节 | Task 5 ReportCard |
| §5.2 流式先 Markdown，合法则换卡片 | Task 2 filter + Task 5 隐藏 text |
| §5.3 CHOICES 仍可用 | Task 2 不剥 CHOICES；Task 5 仍读 text |
| §6.1 JSON 字段与校验 | Task 1 |
| §6.1 yield `report` 事件 | Task 2 |
| §6.2 三种内容写进依据/下一步 | Task 2 prompt |
| §6.3 预填点明 kind | Task 5 chatPrefill |
| §6.4 收藏文件与 POST/DELETE | Task 3–4 |
| §6.5 ChatBlock + ReportCard | Task 4–5 |
| §7 非法标记当普通 Markdown | Task 1 失败返回 None；不 yield report |
| §8 手工验收 | 全任务；由用户执行 |

## Self-review notes

- 未使用 TDD：与仓库约束一致。
- `history` 保留带标记的原文，避免下一轮模型看不到自己写过报告。
- 流式 filter 只扣 REPORT 标记+JSON，不能误伤 `FIN_AGENT_CHOICES_JSON`。
- 有 report 时隐藏 text，CHOICES 仍留在未渲染的 text 块里供 `getQuickReplyOptions` 解析。
