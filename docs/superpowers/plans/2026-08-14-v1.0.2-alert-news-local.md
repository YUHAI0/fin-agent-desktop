# Fin-Agent Desktop v1.0.2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 发布 1.0.2：股价提醒全渠道语义化文案、Agent 全量新闻查询 `query_news`、桌面设置页本地模型（Ollama / LM Studio / 自定义）+ 模型列表自动拉取。

**Architecture:** 保持 Renderer → Electron IPC → `python/api.py` → `fin_agent` 三层。新增 `alert_copy.py`、`news_query.py`、`local_models.py` 三个聚焦模块；`scheduler` / `news_tools` / `ConfigView` 只做接入；`query_notified_news` 不变。

**Tech Stack:** Electron + React + TypeScript、`python/api.py` 自建路由、`fin_agent.scheduler` / `news` / `llm.factory` / `agent.core`、akshare。

**Spec:** `docs/superpowers/specs/2026-08-14-v1.0.2-alert-news-design.md`  
**Roadmap:** `ROADMAP.md` § v1.0.2

## Global Constraints

- 版本号最终为 `1.0.2`（最后一任务改 `VERSION` / `package.json` / 发布说明）。
- 不新增独立服务进程；不实现仪表盘、Watchlist、新闻入库 UI。
- 不生成 Mock 代码；**不编译项目**；实现后由用户手动验收（Verify 为手动清单）。
- 所有面向用户文案使用**简体中文**；提醒文案**必须带股票名称**。
- `query_notified_news` 行为不得改变；`query_news` 不写入 `NotifiedNewsStore`。
- 本地模型统一 `LLM_PROVIDER=local`；子预设通过 `LOCAL_BACKEND` 还原 UI。
- `query_news` 的 `limit` 上限 **50**；板块成分股匹配最多 **30** 只。
- 百分比提醒元数据字段：`alert_mode`、`base_price`、`pct`、`direction`。

---

## File Map

| 文件 | 职责 |
|------|------|
| `python/fin-agent/fin_agent/alert_copy.py` | **新建** 提醒文案生成 |
| `python/fin-agent/fin_agent/scheduler.py` | 触发时用 `format_alert`；pct 创建扩展 |
| `python/fin-agent/fin_agent/alert_history.py` | `_FIELDS` 增加 `message`、`condition_label` |
| `python/api.py` | pct 写元数据；`GET /config/local-models` |
| `python/fin-agent/fin_agent/tools/scheduler_tools.py` | `list_alerts` 输出 `condition_label` |
| `src/renderer/src/components/ReminderTasksModal.tsx` | 展示 `condition_label` / `message` |
| `python/fin-agent/fin_agent/news_query.py` | **新建** 实时新闻拉取与过滤 |
| `python/fin-agent/fin_agent/tools/news_tools.py` | `query_news` 工具 + schema |
| `python/fin-agent/fin_agent/tools/tushare_tools.py` | 注册 `query_news` dispatch |
| `python/fin-agent/fin_agent/agent/core.py` | 新闻工具指引 + 本地模型降级提示 |
| `src/renderer/src/utils/toolDisplayName.ts` | `query_news` 中文名 |
| `python/fin-agent/fin_agent/local_models.py` | **新建** Ollama/LM Studio 模型列表 |
| `python/fin-agent/fin_agent/config.py` | `LOCAL_BACKEND` 读写 |
| `src/renderer/src/components/ConfigView.tsx` | 三本地预设 + 刷新模型 + 能力说明 |
| `src/main/index.ts` + `preload/index.ts` + `env.d.ts` | `listLocalModels` IPC |
| `VERSION` / `docs/RELEASE-NOTES-1.0.2.md` / `ROADMAP.md` | 版本收尾 |

---

### Task 1: `alert_copy.py` 文案模块

**Files:**
- Create: `python/fin-agent/fin_agent/alert_copy.py`

**Interfaces:**
- Produces:
  - `@dataclass AlertCopy: title, body, condition_label, message`
  - `format_alert(task: dict, *, stock_name: str, current_price: float) -> AlertCopy`
  - `format_condition_label(task: dict) -> str` — 列表用，不含 current_price

- [ ] **Step 1: 创建 `alert_copy.py`**

```python
"""股价提醒语义化文案。"""
from dataclasses import dataclass


@dataclass
class AlertCopy:
    title: str
    body: str
    condition_label: str
    message: str


def _fmt_price(value: float) -> str:
    return f"{float(value):.2f}"


def _stock_label(stock_name: str, ts_code: str) -> str:
    name = (stock_name or ts_code or "").strip()
    code = (ts_code or "").strip()
    if name and code and name != code:
        return f"{name}({code})"
    return name or code


def format_condition_label(task: dict) -> str:
    """基于 task 元数据生成条件摘要（列表/Agent，无现价）。"""
    mode = task.get("alert_mode") or "absolute"
    threshold = task.get("threshold")
    operator = task.get("operator") or ">="
    pct = task.get("pct")
    direction = task.get("direction")
    if mode == "pct" and pct is not None and threshold is not None:
        verb = "上涨" if direction == "up" else "下跌"
        return f"{verb} {pct:g}% 至 {_fmt_price(threshold)} 元"
    if threshold is None:
        return "价格提醒"
    t = _fmt_price(threshold)
    if operator == ">=":
        return f"突破 {t} 元"
    if operator == ">":
        return f"超过 {t} 元"
    if operator == "<=":
        return f"回落至 {t} 元及以下"
    if operator == "<":
        return f"跌破 {t} 元"
    return f"价格 {operator} {t} 元"


def format_alert(task: dict, *, stock_name: str, current_price: float) -> AlertCopy:
    ts_code = task.get("ts_code") or ""
    label = _stock_label(stock_name, ts_code)
    price_s = _fmt_price(current_price)
    condition_label = format_condition_label(task)
    mode = task.get("alert_mode") or "absolute"
    threshold = task.get("threshold")

    if mode == "pct" and task.get("base_price") is not None and task.get("pct") is not None:
        base_s = _fmt_price(task["base_price"])
        pct = task["pct"]
        if task.get("direction") == "up":
            message = (
                f"{label} 较设置时（{base_s} 元）上涨 {pct:g}%，"
                f"当前 {price_s} 元，已达目标 {_fmt_price(threshold)} 元"
            )
        else:
            message = (
                f"{label} 较设置时（{base_s} 元）下跌 {pct:g}%，"
                f"当前 {price_s} 元，已达目标 {_fmt_price(threshold)} 元"
            )
    else:
        op = task.get("operator") or ">="
        t = _fmt_price(threshold) if threshold is not None else "—"
        if op == ">=":
            detail = f"价格已突破 {t} 元，当前 {price_s} 元"
        elif op == ">":
            detail = f"价格已超过 {t} 元，当前 {price_s} 元"
        elif op == "<=":
            detail = f"价格已回落到 {t} 元及以下，当前 {price_s} 元"
        else:
            detail = f"价格已跌破 {t} 元，当前 {price_s} 元"
        message = f"{label} {detail}"

    title = f"股价提醒：{label}"
    body = message
    return AlertCopy(title=title, body=body, condition_label=condition_label, message=message)
```

- [ ] **Step 2: 手动验证（Python REPL）**

```python
from fin_agent.alert_copy import format_alert, format_condition_label
task = {"ts_code": "300750.SZ", "operator": ">=", "threshold": 20.5}
assert "突破" in format_condition_label(task)
copy = format_alert(task, stock_name="宁德时代", current_price=21.3)
assert "宁德时代(300750.SZ)" in copy.message
assert "突破" in copy.message
```

- [ ] **Step 3: Commit**

```bash
git add python/fin-agent/fin_agent/alert_copy.py
git commit -m "feat: add alert_copy module for semantic price alert messages"
```

---

### Task 2: Scheduler 接入文案 + 百分比元数据

**Files:**
- Modify: `python/fin-agent/fin_agent/scheduler.py`
- Modify: `python/fin-agent/fin_agent/alert_history.py`
- Modify: `python/api.py` (`handle_price_alert_pct`, `add_price_alert` 路径)

**Interfaces:**
- Consumes: `format_alert`, `format_condition_label` from Task 1
- Produces:
  - `TaskScheduler.add_price_alert(..., alert_mode=None, base_price=None, pct=None, direction=None)`
  - `_check_price_alert` 写入 Toast/邮件/history 时使用 `AlertCopy`
  - `list_tasks_enriched` 每条含 `condition_label: str`

- [ ] **Step 1: 扩展 `add_price_alert` 可选元数据**

在 `scheduler.py` 的 `add_price_alert` 增加可选参数并写入 task dict：

```python
def add_price_alert(
    self, ts_code, operator, threshold, email=None,
    alert_mode=None, base_price=None, pct=None, direction=None,
):
    # ... existing id logic ...
    task = {
        "id": task_id,
        "type": "price_alert",
        "ts_code": ts_code,
        "operator": operator,
        "threshold": float(threshold),
        "email": email or Config.EMAIL_RECEIVER or Config.EMAIL_SENDER,
        "enabled": True,
        "created_at": time.time(),
    }
    if alert_mode:
        task["alert_mode"] = alert_mode
    if base_price is not None:
        task["base_price"] = float(base_price)
    if pct is not None:
        task["pct"] = float(pct)
    if direction:
        task["direction"] = direction
    # save...
```

- [ ] **Step 2: `_check_price_alert` 使用 `format_alert`**

替换现有 `subject` / `desktop_body` / 邮件纯文本中的技术格式：

```python
from fin_agent.alert_copy import format_alert

copy = format_alert(task, stock_name=stock_name, current_price=current_price)
subject = f"[Fin-Agent] {copy.title}"
desktop_body = copy.body
# 邮件 content / html_content 使用 copy.message 作为主描述
AlertHistoryStore().append({
    "task_id": task["id"],
    "ts_code": ts_code,
    "stock_name": stock_name,
    "operator": operator,
    "threshold": threshold,
    "price": current_price,
    "message": copy.message,
    "condition_label": copy.condition_label,
})
self.notification_sink({
    "notification_id": f"price_alert_{task['id']}_{int(time.time())}",
    "type": "price_alert",
    "title": copy.title,
    "body": copy.body,
    # ...
})
```

- [ ] **Step 3: `list_tasks_enriched` 附加 `condition_label`**

```python
from fin_agent.alert_copy import format_condition_label

for t in tasks:
    if t.get("type") == "price_alert":
        t["condition_label"] = format_condition_label(t)
```

- [ ] **Step 4: `alert_history.py` 扩展 `_FIELDS`**

```python
_FIELDS = (
    "id", "task_id", "ts_code", "stock_name", "operator", "threshold",
    "price", "triggered_at", "message", "condition_label",
)
```

- [ ] **Step 5: `api.py` pct 端点写入元数据**

```python
task_id = scheduler.add_price_alert(
    ts_code, operator, threshold, email,
    alert_mode="pct",
    base_price=price,
    pct=pct,
    direction=direction,
)
```

- [ ] **Step 6: Commit**

```bash
git add python/fin-agent/fin_agent/scheduler.py python/fin-agent/fin_agent/alert_history.py python/api.py
git commit -m "feat: wire semantic alert copy and pct task metadata"
```

---

### Task 3: 提醒 UI + Agent `list_alerts`

**Files:**
- Modify: `src/renderer/src/components/ReminderTasksModal.tsx`
- Modify: `python/fin-agent/fin_agent/tools/scheduler_tools.py`

**Interfaces:**
- Consumes: API 返回 `condition_label`、`message`

- [ ] **Step 1: `ReminderTasksModal` 类型与展示**

```typescript
type SchedulerTask = {
  // ...existing...
  condition_label?: string
}
type AlertHistoryRow = {
  // ...existing...
  message?: string
  condition_label?: string
}

function describeTask(t: SchedulerTask): string {
  if (t.type === 'price_alert' && t.ts_code) {
    const name = t.stock_name ? `${t.stock_name} ` : ''
    const cond = t.condition_label || (
      t.operator != null && t.threshold != null
        ? `价格 ${t.operator} ${t.threshold}`
        : ''
    )
    return `${name}${t.ts_code}${cond ? ` · ${cond}` : ''}`
  }
  return t.type ? `${t.type}（${t.id}）` : t.id
}
```

历史列表优先 `item.message`，fallback `价格 {operator} {threshold}`。

- [ ] **Step 2: `list_alerts` 中文输出**

```python
from fin_agent.alert_copy import format_condition_label

for t in tasks:
    if t['type'] == 'price_alert':
        label = format_condition_label(t)
        result += f"- [{status}] {t.get('ts_code')} · {label} (ID: {t['id']})\n"
```

- [ ] **Step 3: Commit**

```bash
git add src/renderer/src/components/ReminderTasksModal.tsx python/fin-agent/fin_agent/tools/scheduler_tools.py
git commit -m "feat: show semantic alert labels in UI and list_alerts"
```

---

### Task 4: `news_query.py` 查询服务

**Files:**
- Create: `python/fin-agent/fin_agent/news_query.py`

**Interfaces:**
- Produces:
  - `query_news_live(query=None, keywords=None, sector=None, sector_mode="both", ts_code=None, sources=None, days=None, hours=None, start_date=None, end_date=None, limit=20) -> dict`
  - 返回 spec §5.4 结构（`items`, `total`, `fetched_from`, `filters_applied`）

- [ ] **Step 1: 创建 `news_query.py` 核心逻辑**

要点实现：

```python
"""按需从 akshare 实时拉取并过滤新闻。"""
import datetime
from fin_agent.news import AkshareNewsAdapter, SUPPORTED_NEWS_SOURCES, SOURCE_STOCK_NEWS_EM
from fin_agent.datasources.normalize import to_ts_code

_MAX_LIMIT = 50
_MAX_CONSTITUENTS = 30

def _parse_published_at(value: str):
    # 解析 ISO；失败返回 None

def _in_time_window(item, *, start_dt, end_dt):
    # published_at 解析失败则保留

def _match_keywords(text: str, needles: list[str]) -> bool:
    # 全部 AND，casefold

def _fetch_constituent_codes(sector: str) -> list[str]:
    import akshare as ak
    df = ak.stock_board_concept_cons_em(symbol=sector)
    # 取代码列，normalize to_ts_code，去重，[:_MAX_CONSTITUENTS]

def query_news_live(...):
    limit = min(max(int(limit or 20), 1), _MAX_LIMIT)
    adapter = AkshareNewsAdapter()
    items = []
    fetched_from = []
    # ts_code → fetch_stock_symbol
    # else → global sources
    # sector constituents / keyword filter
    # dedupe by fingerprint, sort desc, slice limit
    return {"items": [...], "total": len(...), "fetched_from": fetched_from, "filters_applied": {...}}
```

时间窗：若 `start_date`/`end_date` 存在则忽略 `days`/`hours`；否则 `hours` 优先于 `days`。

- [ ] **Step 2: 手动验证（需网络）**

在 Python 进程中调用 `query_news_live(ts_code="300750.SZ", limit=5)`，确认返回 `ok` 结构且 `items` 非空（交易日）。

- [ ] **Step 3: Commit**

```bash
git add python/fin-agent/fin_agent/news_query.py
git commit -m "feat: add news_query live fetch and filter service"
```

---

### Task 5: Agent 工具 `query_news`

**Files:**
- Modify: `python/fin-agent/fin_agent/tools/news_tools.py`
- Modify: `python/fin-agent/fin_agent/tools/tushare_tools.py`
- Modify: `python/fin-agent/fin_agent/agent/core.py`
- Modify: `src/renderer/src/utils/toolDisplayName.ts`

**Interfaces:**
- Consumes: `query_news_live` from Task 4
- Produces: `query_news(...)` 工具函数 + `NEWS_TOOLS_SCHEMA` 新条目

- [ ] **Step 1: `news_tools.py` 增加 `query_news`**

```python
from fin_agent.news_query import query_news_live

def query_news(query=None, keywords=None, sector=None, sector_mode="both",
               ts_code=None, sources=None, days=None, hours=None,
               start_date=None, end_date=None, limit=20):
    try:
        data = query_news_live(
            query=query, keywords=keywords, sector=sector,
            sector_mode=sector_mode, ts_code=ts_code, sources=sources,
            days=days, hours=hours, start_date=start_date, end_date=end_date,
            limit=_limit(limit),  # 复用现有 _limit 但 cap 改为 50
        )
        return _result(data)
    except (TypeError, ValueError, OSError) as exc:
        return _error(f"查询新闻失败：{exc}。可尝试 query_notified_news 查本地已推送新闻。")
```

调整 `_limit` 上限为 50，或新增 `_limit_news_query`。

- [ ] **Step 2: 注册 schema 与 dispatch**

`NEWS_TOOLS_SCHEMA` 追加 `query_news`；`tushare_tools.py` 的 `TOOL_FUNCTIONS` 映射 `"query_news": query_news`。

- [ ] **Step 3: Agent system prompt**

在 `core.py` 新闻指引行改为：

```python
"新闻：查本地已推送 → query_notified_news；按需查全量/个股/板块 → query_news；"
"订阅用 create_news_subscription（持仓订阅不要传 symbols）。\n"
```

- [ ] **Step 4: `toolDisplayName.ts`**

```typescript
query_news: '全量新闻查询',
```

- [ ] **Step 5: Commit**

```bash
git add python/fin-agent/fin_agent/tools/news_tools.py python/fin-agent/fin_agent/tools/tushare_tools.py python/fin-agent/fin_agent/agent/core.py src/renderer/src/utils/toolDisplayName.ts
git commit -m "feat: expose query_news agent tool for live news fetch"
```

---

### Task 6: `local_models.py` + Config API

**Files:**
- Create: `python/fin-agent/fin_agent/local_models.py`
- Modify: `python/fin-agent/fin_agent/config.py`
- Modify: `python/api.py`

**Interfaces:**
- Produces:
  - `normalize_local_base_url(base_url: str, backend: str) -> str`
  - `list_local_models(backend: str, base_url: str, api_key: str | None) -> dict` → `{ok, models?}` or `{ok: False, error}`
  - `Config.LOCAL_BACKEND: str | None`
  - `GET /config` 含 `local_backend`
  - `GET /config/local-models?backend=&base_url=&api_key=`

- [ ] **Step 1: 创建 `local_models.py`**

```python
import requests

def normalize_local_base_url(base_url: str, backend: str) -> str:
    url = (base_url or "").strip().rstrip("/")
    if backend == "ollama":
        return url.removesuffix("/v1") if url.endswith("/v1") else url
    if not url.endswith("/v1"):
        url = f"{url}/v1" if url else url
    return url

def list_local_models(backend: str, base_url: str, api_key: str | None = None) -> dict:
    backend = (backend or "ollama").strip().lower()
    base = normalize_local_base_url(base_url, backend)
    headers = {}
    if api_key:
        headers["Authorization"] = f"Bearer {api_key}"
    try:
        if backend == "ollama":
            root = base.removesuffix("/v1") if base.endswith("/v1") else base
            resp = requests.get(f"{root}/api/tags", timeout=5)
            resp.raise_for_status()
            models = [m.get("name") for m in resp.json().get("models", []) if m.get("name")]
        else:
            resp = requests.get(f"{base}/models", headers=headers, timeout=5)
            resp.raise_for_status()
            models = [m.get("id") for m in resp.json().get("data", []) if m.get("id")]
        return {"ok": True, "models": models}
    except Exception as exc:
        hint = "请确认 Ollama 已启动（ollama serve）" if backend == "ollama" else "请确认 LM Studio 本地服务已开启"
        return {"ok": False, "error": f"无法连接本地模型服务：{exc}。{hint}"}
```

- [ ] **Step 2: `config.py` 增加 `LOCAL_BACKEND`**

- `Config.LOCAL_BACKEND = None`
- `load()` 读 `LOCAL_BACKEND`
- `update_core_config(..., local_backend=None)` 写入 `.env`
- 辅助 `infer_local_backend(base_url) -> str`：`11434`→`ollama`，`1234`→`lmstudio`，否则 `custom`

- [ ] **Step 3: `api.py` 端点**

```python
@route("GET", "/config/local-models")
def handle_local_models(req):
    backend = req.query.get("backend") or "ollama"
    base_url = req.query.get("base_url") or ""
    api_key = req.query.get("api_key") or None
    from fin_agent.local_models import list_local_models
    return list_local_models(backend, base_url, api_key)

# handle_get_config 增加 "local_backend": Config.LOCAL_BACKEND or infer...
# handle_config_save 接受 local_backend 传给 update_core_config
```

- [ ] **Step 4: Commit**

```bash
git add python/fin-agent/fin_agent/local_models.py python/fin-agent/fin_agent/config.py python/api.py
git commit -m "feat: add local model list API and LOCAL_BACKEND config"
```

---

### Task 7: 设置页本地模型 UI + IPC

**Files:**
- Modify: `src/renderer/src/components/ConfigView.tsx`
- Modify: `src/main/index.ts`
- Modify: `src/preload/index.ts`
- Modify: `src/renderer/src/env.d.ts`

**Interfaces:**
- Consumes: `GET /config/local-models`, `local_backend` from config

- [ ] **Step 1: `PROVIDER_PRESETS` 增加三项**

```typescript
local_ollama: {
  label: '本地模型 · Ollama',
  baseUrl: 'http://localhost:11434/v1',
  model: 'qwen2.5:7b',
  keyUrl: 'https://ollama.com/download',
  color: 'amber',
  keyPlaceholder: '可选，默认 ollama',
},
local_lmstudio: {
  label: '本地模型 · LM Studio',
  baseUrl: 'http://localhost:1234/v1',
  model: '',
  keyUrl: 'https://lmstudio.ai/',
  color: 'amber',
  keyPlaceholder: '可选',
},
local_custom: {
  label: '本地模型 · 自定义',
  baseUrl: '',
  model: '',
  keyUrl: '',
  color: 'amber',
  keyPlaceholder: '可选',
},
```

- [ ] **Step 2: `handleProviderChange` / 保存逻辑**

- 选 `local_*` 时：`provider` 存 `'local'`，额外 state `localBackend: 'ollama'|'lmstudio'|'custom'`
- 保存 payload：`provider: 'local'`, `local_backend`, `openai_base`, `openai_model`, `openai_key`（空则填默认 `ollama` / `lm-studio`）
- 加载时：若 `config.provider === 'local'`，下拉选中 `local_${config.local_backend || infer}`

- [ ] **Step 3: 模型列表 UI**

- 本地预设区块：Base URL、API Key（非 required）、「刷新模型列表」按钮、`FaSelect` 模型 + 保留 text input
- 静态提示框（spec §9.4 文案）
- `refreshLocalModels` 调用 `window.api.listLocalModels({ backend, baseUrl, apiKey })`

- [ ] **Step 4: IPC 桥接**

`main/index.ts`:

```typescript
ipcMain.handle('list-local-models', async (_e, payload) =>
  makeApiRequest('/config/local-models', 'GET', undefined, payload as Record<string, string>)
)
```

`preload` + `env.d.ts` 暴露 `listLocalModels`.

- [ ] **Step 5: Commit**

```bash
git add src/renderer/src/components/ConfigView.tsx src/main/index.ts src/preload/index.ts src/renderer/src/env.d.ts
git commit -m "feat: add local LLM presets with model list refresh in settings"
```

---

### Task 8: 本地模型 Agent 降级提示

**Files:**
- Modify: `python/fin-agent/fin_agent/agent/core.py`

**Interfaces:**
- Consumes: `Config.LLM_PROVIDER == "local"`

- [ ] **Step 1: 追加降级文案 helper**

```python
_LOCAL_LLM_HINT = (
    "当前使用的是本地模型，可能不支持或未正确响应工具调用。"
    "建议更换为 Qwen2.5 等支持 function calling 的模型，或在设置中改用 DeepSeek 等云端 API。"
)

def _maybe_append_local_hint(msg: str) -> str:
    from fin_agent.config import Config
    if Config.LLM_PROVIDER != "local":
        return msg
    if _LOCAL_LLM_HINT in msg:
        return msg
    return f"{msg}\n\n{_LOCAL_LLM_HINT}"
```

- [ ] **Step 2: 在 LLM 异常 yield error 处使用**

`stream_chat` 中 `except Exception as e:` 块：

```python
yield {"type": "error", "content": _maybe_append_local_hint(f"Error: {str(e)}")}
```

工具执行失败 `except Exception as e:` 同理追加（可选，至少 LLM 层必做）。

- [ ] **Step 3: Commit**

```bash
git add python/fin-agent/fin_agent/agent/core.py
git commit -m "feat: append local model tool-calling hint on LLM errors"
```

---

### Task 9: 版本收尾与文档

**Files:**
- Modify: `VERSION`（或项目版本文件）
- Modify: `package.json` version
- Create: `docs/RELEASE-NOTES-1.0.2.md`（仓库根或 `fin-agent-desktop/docs/` 与 1.0.1 对齐）
- Modify: `ROADMAP.md` § v1.0.2 勾选完成项
- Modify: spec 状态 → 已完成

- [ ] **Step 1: 版本号 → 1.0.2**

- [ ] **Step 2: 撰写 `RELEASE-NOTES-1.0.2.md`**（提醒文案 / query_news / 本地模型三节）

- [ ] **Step 3: ROADMAP v1.0.2 标记 `[x]`**

- [ ] **Step 4: Commit**

```bash
git add VERSION package.json docs/RELEASE-NOTES-1.0.2.md ROADMAP.md docs/superpowers/specs/2026-08-14-v1.0.2-alert-news-design.md
git commit -m "chore: release v1.0.2 notes and bump version"
```

---

## Manual Acceptance（整体验收）

对照 spec §8：

1. 绝对价提醒 Toast / 邮件 / 历史含股票名称语义文案  
2. 百分比提醒 `tasks.json` 含 pct 元数据；触发文案含「较设置时」  
3. 提醒列表为 `condition_label` 非 `> 20.5`  
4. Agent `query_news(ts_code=...)` 返回实时新闻  
5. Agent `query_news(sector=..., days=3)` 返回板块新闻  
6. `query_notified_news` 不变  
7. 无 Tushare 时 `query_news` 可用  
8. 设置页 Ollama/LM Studio/自定义可刷新模型并保存  
9. 本地模型 LLM 报错含降级提示  

---

## Plan Self-Review

| Spec 要求 | 对应 Task |
|-----------|-----------|
| alert_copy 全渠道 | Task 1–3 |
| pct 元数据 | Task 2 |
| query_news 实时拉取 | Task 4–5 |
| 板块 keyword + 成分股 | Task 4 |
| 时间 days/hours + 绝对日期 | Task 4 |
| 本地三预设 + 模型列表 | Task 6–7 |
| LOCAL_BACKEND | Task 6 |
| 能力说明 + 降级 | Task 7–8 |
| limit 50 / 成分 30 | Task 4 Global Constraints |

无 TBD / 占位符；类型名 `AlertCopy`、`query_news_live`、`list_local_models` 全文一致。
