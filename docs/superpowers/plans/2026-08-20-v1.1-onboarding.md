# v1.1 Onboarding 投资画像向导 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 全新安装时用独立全屏 5 步向导收集投资画像（含金额档位），可跳过；升级用户不弹向导。

**Architecture:** Electron 主进程读配置目录 `onboarding.json` 决定启动 hash（`onboarding` / `chat`）。向导是独立路由 `/onboarding`，草稿仅内存；「开始使用」经现有 `POST /profile` 成功后再标 `completed`。「跳过」先写 `skipped` 再尽力保存草稿。`capital_range` 作为 `user_profile.json` 一等字段。

**Tech Stack:** Electron 主进程 + preload IPC、React + TypeScript、`python/api.py`、`fin_agent.user_profile.UserProfileManager`。

**Spec:** `docs/superpowers/specs/2026-08-20-v1.1-onboarding-design.md`

## Global Constraints

- 仅全新安装出向导；可跳过；之后只靠设置页完整度提示。
- 独立路由 `/onboarding`，不盖在聊天上，不把设置表单当开场。
- 5 步：经验 → 风险 → 周期 → 关注+回避（同一步）→ 金额；回避可空；金额含「暂不透露」。
- 先向导再进主界面；不在向导里配 API。
- 完整度仍只看经验 / 风险 / 周期 / 关注板块。
- 升级检测文件：`user_profile.json`、`.env`、`app_config.json`、`sessions/index.json`。空 `sessions/` 目录不算（`sessions_dir()` 会 mkdir）。
- 面向用户文案用简体中文。
- 不生成 Mock；不编译项目；不写自动化测试；实现后由用户按 spec §10 手工验收。
- 未经用户明确要求不要 git commit。

---

## File Map

| 文件 | 职责 |
|------|------|
| `python/fin-agent/fin_agent/user_profile.py` | `capital_range` 读写、摘要、清除 |
| `python/fin-agent/fin_agent/tools/profile_tools.py` | 工具函数与 schema 增加 `capital_range` |
| `python/api.py` | `POST /profile` 用 `"capital_range" in body` 区分省略与清除 |
| `src/main/onboarding.ts` | **新建** 配置目录、迁移、读写 `onboarding.json`、启动 hash |
| `src/main/index.ts` | 启动加载 hash；IPC skip/complete |
| `src/preload/index.ts` / `src/renderer/src/env.d.ts` | `skipOnboarding` / `completeOnboarding`；`UserProfile.capital_range` |
| `src/renderer/src/utils/profileFields.ts` | **新建** 金额档位、板块预设、拆分「其他」 |
| `src/renderer/src/components/OnboardingView.tsx` | **新建** 5 步向导 |
| `src/renderer/src/index.css` | 向导布局与芯片 |
| `src/renderer/src/App.tsx` | 注册 `/onboarding` |
| `src/renderer/src/components/ProfileView.tsx` | 金额下拉；复用 `profileFields` 拆板块 |

---

### Task 1: 画像后端 `capital_range`

**Files:**
- Modify: `python/fin-agent/fin_agent/user_profile.py`
- Modify: `python/fin-agent/fin_agent/tools/profile_tools.py`
- Modify: `python/api.py`（`handle_profile_save`）

**Interfaces:**
- Consumes: 现有 `UserProfileManager.update_profile(...)`
- Produces:
  - `CAPITAL_RANGES: set[str]`
  - `CAPITAL_RANGE_LABELS: dict[str, str]`
  - `update_profile(..., capital_range=...)`：省略不改；`None`/`""` 删除字段；合法键写入
  - `get_profile_summary()` 含「可投资金额」行
  - `POST /profile` body 可带 `capital_range`

- [ ] **Step 1: 在 `user_profile.py` 顶部常量后（`class UserProfileManager` 之前）加入档位表，并改 `update_profile` / `get_profile_summary`**

`_default_profile` **不要**加入 `capital_range`。`completeness()` **不要**把金额算进 missing。

```python
CAPITAL_RANGES = {
    "under_5w",
    "5_20w",
    "20_50w",
    "50_100w",
    "over_100w",
    "undisclosed",
}

CAPITAL_RANGE_LABELS = {
    "under_5w": "5万以下",
    "5_20w": "5–20万",
    "20_50w": "20–50万",
    "50_100w": "50–100万",
    "over_100w": "100万以上",
    "undisclosed": "暂不透露",
}

_UNSET = object()
```

将 `update_profile` 签名改为增加 `capital_range=_UNSET`（放在 `experience_level` 与 `**kwargs` 之间）。在 `experience_level` 写入逻辑之后、`kwargs` 之前加入：

```python
        if capital_range is not _UNSET:
            if capital_range in (None, ""):
                self.profile.pop("capital_range", None)
            else:
                if capital_range not in CAPITAL_RANGES:
                    raise ValueError("capital_range 无效")
                self.profile["capital_range"] = capital_range
```

在 `get_profile_summary` 里 `investment_style` 那一行后追加：

```python
        capital = p.get("capital_range")
        if not capital:
            capital_label = "未指定"
        elif capital in CAPITAL_RANGE_LABELS:
            capital_label = CAPITAL_RANGE_LABELS[capital]
        else:
            capital_label = str(capital)
```

并在摘要字符串中增加：

```text
- 可投资金额：{capital_label}
```

（放在「投资风格」行附近。）

- [ ] **Step 2: 扩展 `profile_tools.py`**

`update_user_profile` 增加参数 `capital_range=None`，并传给 `update_profile`。注意：工具调用省略该参数时不应清除字段。用与 Manager 相同的 `_UNSET` 不便跨模块；改为：

```python
def update_user_profile(
    risk_tolerance=None,
    investment_horizon=None,
    favorite_sectors=None,
    avoid_sectors=None,
    investment_style=None,
    experience_level=None,
    capital_range=_UNSET,
):
    kwargs = {}
    if capital_range is not _UNSET:
        kwargs["capital_range"] = capital_range
    return get_profile_manager().update_profile(
        risk_tolerance=risk_tolerance,
        investment_horizon=investment_horizon,
        favorite_sectors=favorite_sectors,
        avoid_sectors=avoid_sectors,
        investment_style=investment_style,
        experience_level=experience_level,
        **kwargs
    )
```

在本文件 `from fin_agent.user_profile import UserProfileManager` 改为同时导入 `_UNSET` 不合适（若 `_UNSET` 不导出）。**导出** `user_profile.py` 的 `_UNSET` 为 `UNSET = _UNSET`，或在 `profile_tools.py` 自己 `UNSET = object()` —— **不要**。工具层若默认 `capital_range=None`，LLM 省略参数会变成 `None` 从而误删字段。

因此：`profile_tools.update_user_profile` 必须用独立 sentinel：

```python
from fin_agent.user_profile import UserProfileManager, UNSET
```

在 `user_profile.py` 增加 `UNSET = _UNSET`（同一 object）。

`PROFILE_TOOLS_SCHEMA` 的 `update_user_profile.parameters.properties` 增加：

```python
                    "capital_range": {
                        "type": "string",
                        "enum": [
                            "under_5w",
                            "5_20w",
                            "20_50w",
                            "50_100w",
                            "over_100w",
                            "undisclosed",
                        ],
                        "description": "Investable capital range bucket. Omit to leave unchanged.",
                    },
```

`tushare_tools.py` 里 `update_user_profile(**arguments)` 无需改：合法键会传入。

- [ ] **Step 3: 改 `python/api.py` 的 `handle_profile_save`**

`body.get("capital_range")` 无法区分省略与 `null`。改为：

```python
        kwargs = {}
        if "capital_range" in body:
            kwargs["capital_range"] = body.get("capital_range")
        pm.update_profile(
            risk_tolerance=body.get("risk_tolerance"),
            investment_horizon=body.get("investment_horizon"),
            favorite_sectors=body.get("favorite_sectors"),
            avoid_sectors=body.get("avoid_sectors"),
            investment_style=body.get("investment_style"),
            experience_level=body.get("experience_level"),
            **kwargs,
        )
```

- [ ] **Step 4: 手动核对（本任务）**

不启动编译。确认：`completeness()` 源码未把 `capital_range` 加入 `missing`；缺省 profile 无该键。

---

### Task 2: 主进程启动分流

**Files:**
- Create: `src/main/onboarding.ts`
- Modify: `src/main/index.ts`（`createChatWindow` 的 `loadURL` / `loadFile`；注册 IPC）
- Modify: `src/preload/index.ts`
- Modify: `src/renderer/src/env.d.ts`

**Interfaces:**
- Consumes: `app.getPath('appData'|'home')`、`process.platform`、`process.env.XDG_CONFIG_HOME`
- Produces:
  - `finAgentConfigDir(): string`
  - `resolveStartupHash(): 'onboarding' | 'chat'`
  - `markOnboardingStatus(status: 'completed' | 'skipped'): void`
  - IPC `skip-onboarding` / `complete-onboarding`
  - preload: `skipOnboarding(): Promise<void>`、`completeOnboarding(): Promise<void>`

- [ ] **Step 1: 新建 `src/main/onboarding.ts`**

配置目录算法对齐 Python `Config.get_config_dir()`：

- Windows: `join(app.getPath('appData'), 'fin-agent')`
- 其他: `XDG_CONFIG_HOME` 或 `join(homedir, '.config', 'fin-agent')`（`homedir` 用 `app.getPath('home')`）

```ts
import { app } from 'electron'
import { existsSync, mkdirSync, readFileSync, writeFileSync } from 'fs'
import { join } from 'path'
import { homedir } from 'os'

export type OnboardingDiskStatus = 'pending' | 'completed' | 'skipped' | 'migrated'

export function finAgentConfigDir(): string {
  if (process.platform === 'win32') {
    return join(app.getPath('appData'), 'fin-agent')
  }
  const xdg = process.env.XDG_CONFIG_HOME
  if (xdg) return join(xdg, 'fin-agent')
  return join(app.getPath('home') || homedir(), '.config', 'fin-agent')
}

function onboardingPath(): string {
  return join(finAgentConfigDir(), 'onboarding.json')
}

function hasLegacyData(dir: string): boolean {
  return (
    existsSync(join(dir, 'user_profile.json')) ||
    existsSync(join(dir, '.env')) ||
    existsSync(join(dir, 'app_config.json')) ||
    existsSync(join(dir, 'sessions', 'index.json'))
  )
}

function writeStatus(status: OnboardingDiskStatus): void {
  const dir = finAgentConfigDir()
  mkdirSync(dir, { recursive: true })
  writeFileSync(
    onboardingPath(),
    JSON.stringify({ status, updated_at: new Date().toISOString() }, null, 2),
    'utf8'
  )
}

export function markOnboardingStatus(status: 'completed' | 'skipped'): void {
  writeStatus(status)
}

export function resolveStartupHash(): 'onboarding' | 'chat' {
  try {
    const dir = finAgentConfigDir()
    const file = onboardingPath()
    if (!existsSync(file)) {
      if (hasLegacyData(dir)) {
        writeStatus('migrated')
        return 'chat'
      }
      return 'onboarding'
    }
    const raw = JSON.parse(readFileSync(file, 'utf8'))
    const status = raw && typeof raw.status === 'string' ? raw.status : ''
    if (status === 'completed' || status === 'skipped' || status === 'migrated') {
      return 'chat'
    }
    return 'onboarding'
  } catch (err) {
    console.warn('[Onboarding] resolveStartupHash failed, falling back to chat:', err)
    return 'chat'
  }
}
```

- [ ] **Step 2: 改 `createChatWindow` 加载地址**

在 `src/main/index.ts` 增加 import：

```ts
import { markOnboardingStatus, resolveStartupHash } from './onboarding'
```

把窗口加载替换为：

```ts
  const startupHash = resolveStartupHash()
  if (is.dev && process.env['ELECTRON_RENDERER_URL']) {
    chatWindow.loadURL(`${process.env['ELECTRON_RENDERER_URL']}/#/${startupHash}`)
  } else {
    chatWindow.loadFile(join(__dirname, '../renderer/index.html'), { hash: startupHash })
  }
```

`App.tsx` 的 `/` 仍指向 `InputView`，不要改成 Onboarding。

- [ ] **Step 3: IPC + preload + 类型**

`index.ts` 在现有 `save-profile` 附近增加：

```ts
  ipcMain.handle('skip-onboarding', () => {
    markOnboardingStatus('skipped')
    return { success: true }
  })
  ipcMain.handle('complete-onboarding', () => {
    markOnboardingStatus('completed')
    return { success: true }
  })
```

`preload/index.ts` 的 `getProfile` 旁：

```ts
  skipOnboarding: () => ipcRenderer.invoke('skip-onboarding'),
  completeOnboarding: () => ipcRenderer.invoke('complete-onboarding'),
```

`env.d.ts` 的 `UserProfile` 增加可选字段：

```ts
  capital_range?: 'under_5w' | '5_20w' | '20_50w' | '50_100w' | 'over_100w' | 'undisclosed'
```

并在 `saveProfile` 旁：

```ts
    skipOnboarding: () => Promise<{ success: boolean }>
    completeOnboarding: () => Promise<{ success: boolean }>
```

- [ ] **Step 4: 手动核对（本任务）**

确认 `createChatWindow` 不再写死 `#/chat`。不要编译。

---

### Task 3: 向导页

**Files:**
- Create: `src/renderer/src/utils/profileFields.ts`
- Create: `src/renderer/src/components/OnboardingView.tsx`
- Modify: `src/renderer/src/App.tsx`
- Modify: `src/renderer/src/index.css`

**Interfaces:**
- Consumes: `window.api.saveProfile`、`skipOnboarding`、`completeOnboarding`、Task 1 的 `capital_range` API
- Produces: 路由 `/onboarding`；完成/跳过进入 `/chat`

- [ ] **Step 1: 新建 `src/renderer/src/utils/profileFields.ts`**

```ts
export type CapitalRange =
  | 'under_5w'
  | '5_20w'
  | '20_50w'
  | '50_100w'
  | 'over_100w'
  | 'undisclosed'

export const CAPITAL_RANGE_OPTIONS: { value: CapitalRange; label: string }[] = [
  { value: 'under_5w', label: '5 万以下' },
  { value: '5_20w', label: '5–20 万' },
  { value: '20_50w', label: '20–50 万' },
  { value: '50_100w', label: '50–100 万' },
  { value: 'over_100w', label: '100 万以上' },
  { value: 'undisclosed', label: '暂不透露' }
]

export const SECTOR_PRESETS = [
  '新能源',
  '半导体',
  '消费',
  '医药',
  '金融',
  '军工',
  '周期',
  '科技',
  '红利',
  '大盘蓝筹'
] as const

export function parseSectorList(raw: string): string[] {
  return raw
    .split(/[,，、]/)
    .map((s) => s.trim())
    .filter(Boolean)
}

export function mergeSectors(selected: string[], extraRaw: string): string[] {
  const extra = parseSectorList(extraRaw)
  const out: string[] = []
  for (const name of [...selected, ...extra]) {
    if (!out.includes(name)) out.push(name)
  }
  return out
}
```

注意：spec 要求关注/回避**跨列表不去重**；`mergeSectors` 只对**同一列表内部**去重。

- [ ] **Step 2: 新建 `OnboardingView.tsx`**

用 `SubPageShell` + `fa-page-header`（可拖拽、平台留位）。无返回聊天按钮。右上「跳过」。

状态：

```ts
const [step, setStep] = useState(0) // 0..4
const [experienceLevel, setExperienceLevel] = useState<'' | 'beginner' | 'experienced'>('')
const [riskTolerance, setRiskTolerance] = useState<'' | 'Conservative' | 'Balanced' | 'Aggressive'>('')
const [investmentHorizon, setInvestmentHorizon] = useState<'' | 'Short-term' | 'Medium-term' | 'Long-term'>('')
const [favoriteSelected, setFavoriteSelected] = useState<string[]>([])
const [avoidSelected, setAvoidSelected] = useState<string[]>([])
const [favoriteExtra, setFavoriteExtra] = useState('')
const [avoidExtra, setAvoidExtra] = useState('')
const [capitalRange, setCapitalRange] = useState<CapitalRange | ''>('')
const [busy, setBusy] = useState(false)
const [error, setError] = useState('')
```

`canNext`：

- step 0: `experienceLevel !== ''`
- step 1: `riskTolerance !== ''`
- step 2: `investmentHorizon !== ''`
- step 3: `mergeSectors(favoriteSelected, favoriteExtra).length >= 1`
- step 4: `capitalRange !== ''`

`buildPayload()`：只包含已填字段（跳过时用）。完成时 5 项都有（回避可为 `[]`）。

```ts
function buildPayload(mode: 'skip' | 'complete'): Record<string, unknown> {
  const data: Record<string, unknown> = {}
  if (experienceLevel) data.experience_level = experienceLevel
  if (riskTolerance) data.risk_tolerance = riskTolerance
  if (investmentHorizon) data.investment_horizon = investmentHorizon
  const fav = mergeSectors(favoriteSelected, favoriteExtra)
  const avd = mergeSectors(avoidSelected, avoidExtra)
  if (mode === 'complete' || fav.length) data.favorite_sectors = fav
  if (mode === 'complete' || avd.length) data.avoid_sectors = avd
  if (capitalRange) data.capital_range = capitalRange
  return data
}

function hasDraft(): boolean {
  return Object.keys(buildPayload('skip')).length > 0
}

async function saveProfileWithRetry(data: Record<string, unknown>): Promise<{
  success: boolean
  error?: string
}> {
  const delays = [0, 400, 800, 1200, 1600, 2000, 2000, 2000]
  let lastError = '保存失败'
  for (const wait of delays) {
    if (wait) await new Promise((r) => setTimeout(r, wait))
    try {
      const result = await window.api.saveProfile(data)
      if (result?.success) return { success: true }
      lastError = result?.error || lastError
    } catch (e: any) {
      lastError = e?.message || lastError
    }
  }
  return { success: false, error: lastError }
}

async function handleSkip() {
  setBusy(true)
  setError('')
  try {
    await window.api.skipOnboarding()
    const draft = buildPayload('skip')
    if (Object.keys(draft).length > 0) {
      await saveProfileWithRetry(draft) // 失败忽略
    }
    navigate('/chat')
  } catch (e: any) {
    setError(e?.message || '无法跳过，请重试')
    setBusy(false)
  }
}

async function handleComplete() {
  if (capitalRange === '' || !canNext) return
  setBusy(true)
  setError('')
  const result = await saveProfileWithRetry(buildPayload('complete'))
  if (!result.success) {
    setError(result.error || '画像保存失败，请重试')
    setBusy(false)
    return
  }
  await window.api.completeOnboarding()
  navigate('/chat')
}
```

完成必须 **先** `saveProfile` 成功 **再** `completeOnboarding`。跳过必须 **先** `skipOnboarding` **再** 尝试保存（保存失败仍 `navigate('/chat')`）。

步骤 UI：每步一组大号选项按钮（`fa-onboarding-choice`，选中用 `data-active`）。第 4 步两行芯片：关注、回避；其下各一个「其他」input。第 5 步用 `CAPITAL_RANGE_OPTIONS`。

文案：标题「先认识一下你」；副标题随 step 变化。第 5 步主按钮「开始使用」，等待时「保存中…」。

`useNavigate` from `react-router-dom`。

- [ ] **Step 3: `App.tsx` 增加路由**

```tsx
import OnboardingView from './components/OnboardingView'
```

```tsx
        <Route path="/onboarding" element={<OnboardingView />} />
```

放在 `/chat` 旁。不要改 `/`。

- [ ] **Step 4: `index.css` 增加向导样式（`@layer components` 内，靠近 `.fa-page`）**

```css
  .fa-onboarding-progress {
    @apply mx-auto mb-8 flex max-w-md gap-1.5;
  }

  .fa-onboarding-progress span {
    @apply h-1 flex-1 rounded-full;
    background: var(--fa-border-subtle);
  }

  .fa-onboarding-progress span[data-done='true'] {
    background: var(--fa-accent);
  }

  .fa-onboarding-choices {
    @apply flex flex-col gap-2;
  }

  .fa-onboarding-choice {
    @apply w-full cursor-pointer rounded-xl px-4 py-3 text-left text-sm font-medium;
    border: 1px solid var(--fa-border);
    background: var(--fa-surface);
    color: var(--fa-text);
  }

  .fa-onboarding-choice[data-active='true'] {
    border-color: var(--fa-accent);
    background: var(--fa-accent-soft);
    color: var(--fa-sidebar-active-text);
  }

  .fa-onboarding-chips {
    @apply flex flex-wrap gap-2;
  }

  .fa-onboarding-chip {
    @apply cursor-pointer rounded-full px-3 py-1.5 text-sm;
    border: 1px solid var(--fa-border);
    background: transparent;
    color: var(--fa-muted);
  }

  .fa-onboarding-chip[data-active='true'] {
    border-color: var(--fa-accent);
    background: var(--fa-accent-soft);
    color: var(--fa-text);
  }

  .fa-onboarding-footer {
    @apply mt-10 flex items-center justify-between gap-3;
  }
```

浅色主题下 `fa-onboarding-choice[data-active='true']` 的文字若对比不足，用 `color: var(--fa-text)` 覆盖。

- [ ] **Step 5: 手动核对（本任务）**

向导页能渲染 5 步；未选时下一步 disabled。不编译。

---

### Task 4: 设置页金额档位

**Files:**
- Modify: `src/renderer/src/components/ProfileView.tsx`

**Interfaces:**
- Consumes: `CAPITAL_RANGE_OPTIONS`、`parseSectorList`（可选，替换本地 `parseSectors`）
- Produces: 保存/加载 `capital_range`；「未设置」发送 `capital_range: null`

- [ ] **Step 1: 改 `ProfileView.tsx`**

增加：

```ts
import { CAPITAL_RANGE_OPTIONS, parseSectorList, type CapitalRange } from '../utils/profileFields'
```

本地 `parseSectors` 改为调用 `parseSectorList`（或删除本地函数）。

状态：`capitalRange: CapitalRange | ''`，`''` = 未设置。

`applyProfileResult`：若 `profile.capital_range` 落在 `CAPITAL_RANGE_OPTIONS` 则选中，否则 `''`。

`handleSave` 的 data：

```ts
        capital_range: capitalRange === '' ? null : capitalRange,
```

必须带该键，以便清除。

UI：在投资周期与关注板块之间插入：

```tsx
            <div className="space-y-2">
              <label className="fa-label">可投资金额</label>
              <FaSelect
                value={capitalRange}
                aria-label="可投资金额"
                onChange={(v) => setCapitalRange(v as CapitalRange | '')}
                options={[{ value: '', label: '未设置' }, ...CAPITAL_RANGE_OPTIONS]}
              />
            </div>
```

确认 `FaSelect` 的 `options` 类型允许 `value: ''`。若不允许，用 `'unset'` 并在保存时映射为 `null`。

完整度区块不要因金额改变文案逻辑。

- [ ] **Step 2: 对照 spec §10 列出手工验收清单（实现者完成后停）**

按 `docs/superpowers/specs/2026-08-20-v1.1-onboarding-design.md` §10 六条，交给用户手动测。不要编译、不要写测试、不要 Mock。

---

## Spec coverage

| Spec | Task |
|------|------|
| §4 启动分流 / migrated / pending | Task 2 |
| 空 sessions 目录不算升级 | Task 2 `hasLegacyData` 只看 `sessions/index.json` |
| `/` 不兜底向导 | Task 2 不改 `/` |
| §5 五步 UI、跳过/上下步 | Task 3 |
| §5.2 板块芯片 + 其他 | Task 3 |
| §5.3 金额键 | Task 1 + Task 3 |
| §6 跳过先标 skipped；完成先保存再 completed | Task 3 |
| Python 未就绪重试 | Task 3 `saveProfileWithRetry` |
| §7 capital_range 省略/清除/校验 | Task 1 |
| completeness 不变 | Task 1 |
| 摘要含金额 | Task 1 |
| 工具 schema | Task 1 |
| §8 设置页 | Task 4 |
| 仪表盘/报告/Watchlist | 不做 |

## Self-review notes

- 未使用 TDD：spec 与仓库约束禁止自动测试与 Mock。
- `profile_tools` 的 `capital_range` 必须用 `UNSET`，不能默认 `None`。
- `POST /profile` 必须用 `"capital_range" in body`。
